# Alton Education — 프로젝트 컨텍스트

이 파일은 Claude Code가 세션을 시작할 때 자동으로 읽는 파일입니다. 새 세션마다 이 내용을 다시 설명할 필요가 없도록 여기 유지하세요.

## 현재 작업 기준 — 반드시 먼저 읽기

서비스는 아직 오픈 전이며 운영 고객 데이터는 없다. 기존 구현은 검증용 개발본이다.

**새 세션은 과거 문서를 전부 읽지 말고 다음 순서로만 시작한다**:

1. `CLAUDE.md`(이 파일)
2. `docs/CURRENT.md` — 지금 완료된 단계, 현재 스키마·외부 서비스 구조, 다음 작업에 필요한 확정 정책, 최신 migration, 배포 상태, 남은 blocker를 1~2페이지로 압축한 "지금 상태" 요약. **매 R 단계 종료 시 갱신된다 — 항상 이 문서를 최신 진행 상태의 출발점으로 삼는다.**
3. 지금 진행할 R 단계의 `docs/2026-08-29-master-roadmap-v3.md` 해당 섹션
4. 그 작업에 직접 필요한 설계 문서만 선택적으로 확인(예: `docs/2026-08-29-product-architecture-v3.md`의 관련 절, 해당 Gate 문서)

`docs/2026-08-29-r0-approval-and-technical-validation-package.md`(Gate A·B·C 상세)와 각 R의 실행 로그(`docs/2026-08-29-r*-migration-execution-log.md`), `docs/tickets.md`, `docs/prompts/`, `docs/spec/schema-draft.md`, v2 문서, `docs/superpowers/`의 기존 계획은 **필요할 때만 검색**한다 — 세션 시작 시 통째로 읽지 않는다. 문서가 충돌하면 `docs/CURRENT.md` → 해당 R 로드맵 섹션 → 원본 정책 문서 순으로 더 최신·구체적인 쪽이 우선한다. 전체 문서 분류는 `docs/README.md` 참고.

## 세션 역할과 효율적 전달 방식

사용자가 지정한 역할을 세션 전체에서 유지한다. 역할 밖의 작업을 선의로 확장하지 않는다.

- **기획 세션**: 개발자 현황을 `CURRENT.md`·로드맵·확정 정책과 대조하고, 다음 개발 지시를 한 번에 작성한다. 코드 수정, 테스트 실행, 마이그레이션, 배포, 외부 서비스 조작은 하지 않는다. 기술적 선택지는 개발자에게 맡기고, 사용자 경험·정책·비용·위험이 달라지는 결정만 사용자에게 질문한다.
- **개발 세션**: 승인된 R 범위의 구현·테스트·additive migration·개발 DB 반영·문서 동기화를 연속 수행한다. 구현 순서나 일반적인 기술 선택을 매번 되묻지 않는다. 계약 문안이나 미확정 제품 정책을 임의로 만들지 않는다.
- **계약 문안 세션**: 계약서·부속 문서와 미결정 법률·운영 항목만 다룬다. 코드, DB, 배포, DocuSign 발송·연동 테스트는 하지 않는다.

기본 전달 구조는 `개발자 보고 → 사용자 전달 → 기획자 검수·통합 지시 → 사용자 전달 → 개발자 실행`이다. 기획자는 같은 배경을 반복하지 않고 변경된 결정과 다음 작업만 전달한다. 개발자는 승인된 범위 안에서 작업을 이어가며 아래의 중단 조건이 생길 때만 질문한다.

### 토큰·핑퐁 절감 규칙

- 시작 요약은 10줄 이내, 진행 보고는 변경점만 쓴다. 이미 확정된 배경과 정책을 다시 설명하지 않는다.
- 검색은 먼저 파일명·제목·키워드로 범위를 좁히고 필요한 절만 읽는다. 전체 로그·과거 계획·대형 문서를 통째로 읽지 않는다.
- 명령 출력은 성공 요약과 실패 원인만 보고한다. 전체 로그는 실패 분석에 필요할 때만 포함한다.
- 승인된 R 안의 일반 조사·구현·수정·테스트는 묶어서 끝낸다. 선택지를 나열해 사용자에게 기술 결정을 떠넘기지 않는다.
- 서로 독립적인 작업만 병렬화한다. 같은 파일·마이그레이션·정책을 건드리는 작업은 한 세션에서 순차 처리한다.
- 기존 미커밋 변경은 사용자 소유로 간주한다. 새 작업 전 관련 변경과 무관한 변경을 구분하고, 현재 업무 파일만 수정·검증·커밋한다.

### 개발자 보고 형식

중간 보고는 blocker가 있을 때만 한다. 그 외에는 작업 단위 완료 후 아래 다섯 항목으로 한 번만 보고한다.

1. **완료** — 인수 기준 중 끝난 항목
2. **검증** — 실행한 테스트와 실제 결과
3. **미완료** — 남은 항목과 다음 자동 진행 순서
4. **결정 필요** — 사용자 결정이 필요한 정책 질문만, 권장안과 영향 포함
5. **외부 변경** — 실제 외부 읽기·쓰기·발송·배포 여부와 플래그 복원 상태

기획자는 이 보고를 받은 뒤 누락·정책 충돌을 확인하고, 개발자에게 보낼 **하나의 통합 지시문**만 작성한다.

Gate A·B·C는 2026-08-30 전부 완료됐다(Gate B: `docs/2026-08-29-gate-b-migration-and-permission-design.md` v5. Gate C: `docs/2026-08-29-gate-c-google-workspace-validation.md`, `ALTON Integration Sandbox`에서 GW-01~13 Google 기술 검증 완료, 데이터 모델 blocker 없음). **R1 이후 구현 착수 가능.** 다만 Gate C 검증 중 발견된 3가지 ALTON 앱 워크플로우 중 GW-12(`manual_review` 큐 적재·재처리)와 GW-14(보존·삭제 자동화)는 앱이 없어 Gate C에서 검증하지 못했으므로 삭제하지 않고 `docs/2026-08-29-master-roadmap-v3.md`의 R8/R12 필수 인수 기준(blocker)으로 이관했다 — 해당 R 단계를 완료로 보고하기 전에 반드시 통과시킨다. **GW-10(보호자 거부 시 수동 리뷰 task)은 2026-09-03 정책 변경으로 폐기됐다** — Smart Notes가 보호자 opt-out 선택 기능에서 가족 서비스 이용계약의 필수 조항으로 바뀌면서 "거부"라는 선택지 자체가 없어졌다(`docs/CURRENT.md` R6 16/N 참고).

## 프로젝트 개요

**Alton Education Inc.** — 한국/미국 거주 중이며 미국 명문대 진학을 목표로 하는 국제학교/미국 현지 중고등학생(한국인/아시아인 타겟)을 위한 프리미엄 비대면 SAT/AP 사교육 플랫폼.

- 학생을 모집하고 선생님(한국 명문대 대학원생)을 발굴·매칭하되, 단순 중개가 아니라 하나의 교육 브랜드로 교육 퀄리티와 운영 구조를 자체적으로 관리하는 "비대면 유학 사교육 학원"
- 4개 포털: 학생 / 학부모 / 선생님 / 관리자
- 핵심 차별점: "수업 세션뷰" — 교재(TOC+아티클), 캔버스 필기, 단어장, AI 문제생성, 과제, 연습장(Docs/화이트보드)이 통합된 학생×선생님×회차 귀속 화면

## 레퍼런스 — 기존 목업

`docs/spec/mockups/`의 HTML 파일은 기존 화면의 시각 구조와 핵심 상호작용을 확인하는 기준이다. 다만 예약, 계약, 수업권, 선생님 변경, Google Workspace처럼 v3에서 바뀐 흐름은 목업을 그대로 구현하지 않는다. 목업과 v3가 충돌하면 v3 정책을 따르고 필요한 화면을 다시 설계한다.

`docs/spec/functional-spec.md`는 포털 역할과 교재·세션 분리의 역사적 배경을 확인할 때만 참고한다. Calendly, Zoom, DocuSign, 크레딧, 파일럿 범위 등 그 문서의 오래된 정책은 사용하지 않는다.

- `alton_landing_v5.html` — 랜딩 페이지
- `alton_auth_v3.html` — 로그인/회원가입
- `alton_admin_portal_v6.html` — 관리자 포털
- `alton_teacher_portal_v12.html` — 선생님 포털
- `alton_student_portal_v16.html` — 학생 포털
- `alton_parent_portal_v9.html` — 학부모 포털
- `alton_material_viewer_prototype.html` — **수업 세션뷰** (가장 중요, 4개 포털에서 전부 진입하는 공통 화면)

## 아키텍처 원칙 (목업에서 확립된 것)

1. **레이어 분리**: "수업 세션"(학생×선생님×회차에 귀속)과 "교재"(관리자가 만든 콘텐츠, 세션과 무관하게 존재)는 명확히 다른 레이어다. 교재 라이브러리에서 교재를 보는 것과, 특정 수업 세션에서 배정된 교재를 보는 것은 다른 화면/다른 데이터여야 한다.
2. **세션뷰는 역할에 따라 같은 화면, 다른 권한**: 학생/선생님이 보는 화면 구조는 같지만 편집 권한(코멘트 수정, 문제 생성 등)이 다르다. URL 파라미터로 흉내낸 부분(`?role=`, `?state=`)은 실제로는 로그인 세션 + DB 관계로 대체되어야 한다.
3. **문제 생성(AI)은 선생님 전용 도구다.** 생성 → 편집 가능한 초안 → 재생성 → "과제로 확정" 절차를 거쳐야 학생에게 노출된다. 학생/학부모에게는 이 기능 자체가 보이지 않아야 한다.
4. **과목 템플릿이 단일 진실 소스(source of truth)**: 관리자가 과목/단원을 과목 템플릿에 추가하면, 교재 생성 폼 등 다른 곳의 선택지가 자동으로 갱신되어야 한다 (하드코딩 목록 금지).

## 기술 스택 (결정됨)

- **프레임워크**: Next.js (App Router), TypeScript
- **DB / Auth / 실시간·임시 Storage**: Supabase (Postgres)
- **확정 문서·수업자료**: 회사 소유 Google Shared Drive
- **배포**: Vercel
- **스타일**: Tailwind CSS — 목업의 CSS 변수(`--ink`, `--grey-500`, `--red`, `--green` 등)를 그대로 Tailwind 커스텀 테마로 이식
- **테스트**: Vitest (유닛) + Playwright (E2E) — 목업 단계에서 jsdom으로 검증하던 습관을 그대로 이어간다. 기능을 완성했다고 보고하기 전에 반드시 관련 테스트를 실행하고 통과를 확인한다.

## 코딩 컨벤션

- 컴포넌트는 기능 단위 폴더로 (`app/(session)/session-view/`, `app/admin/curriculum/` 등)
- 목업의 한국어 UI 텍스트/라벨은 그대로 유지한다 (번역하지 않음)
- 데이터 모델 필드명은 목업의 변수명을 참고하되, DB 컬럼은 snake_case로 정규화
- v3 범위 밖 기능이나 정책을 임의로 추가하지 않는다. 목업에 없더라도 v3에 확정된 기능은 구현 범위다.

## 작업 방식

- 신규 작업은 `docs/2026-08-29-master-roadmap-v3.md`의 R0→R13 의존성과 게이트 순서를 따른다. **현재 진행 상태는 `docs/CURRENT.md`를 최신으로 신뢰한다.**
- Gate A·B·C, R1, R2 전부 완료(`docs/CURRENT.md` 참고). R8/R12 작업 시 Gate C에서 이관된 인수 기준(GW-12/14 관련)을 빠뜨리지 말 것 — 위 문단 참고(GW-10은 폐기됨).
- 화면 작업 전 관련 목업과 v3 정책을 함께 읽고, 충돌하면 v3 정책을 적용한다.
- **승인 단위는 R(마일스톤) 단위**: 각 R 착수 시 목표·핵심 사용자 흐름 변경·DB·코드·외부 서비스 범위·기존 기능 영향·완료 기준·실제 외부 변경 여부를 한 번 공유하고 승인받는다. 그 안의 조사·구현·additive migration·로컬 검증·개발 DB 반영·배포·문서 동기화는 반복 승인 없이 연속 진행한다. 순수 버그 수정·누락된 권한 검사 보완·테스트 추가·오류 메시지 정정·additive migration·테스트 데이터 정리는 자체 판단으로 처리한다.
- **반드시 멈추고 확인**: 확정된 정책을 실질적으로 바꿔야 할 때, 비용·유료 계약이 걸릴 때, 실제 외부 계정·데이터를 삭제/복구 불가능하게 만들 때, 실제 사용자에게 이메일·알림이 나갈 때, 운영 데이터 손실 가능성이 있는 파괴적 마이그레이션일 때, 승인된 외부 쓰기 범위를 벗어날 때, 사용자 경험·정산 결과가 선택지에 따라 달라질 때.
- 완료 후 관련 테스트(구현 중엔 변경 영역 위주, DB/RLS 변경 시 역할·상태·동시성, R 종료 시 전체 테스트+핵심 E2E 1회)와 문서·마이그레이션·운영 재처리까지 검증하고 실제 결과를 보고한다.
- 정책 또는 권한이 불명확하면 구형 문서에서 답을 가져오지 말고 질문한다. 단, 결과에 영향을 주지 않는 기술 구현 선택은 개발자가 판단한다.
