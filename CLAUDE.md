# Alton Education — 프로젝트 컨텍스트

이 파일은 Claude Code가 세션을 시작할 때 자동으로 읽는 파일입니다. 새 세션마다 이 내용을 다시 설명할 필요가 없도록 여기 유지하세요.

## 프로젝트 개요

**Alton Education Inc.** — 한국/미국 거주 중이며 미국 명문대 진학을 목표로 하는 국제학교/미국 현지 중고등학생(한국인/아시아인 타겟)을 위한 프리미엄 비대면 SAT/AP 사교육 플랫폼.

- 학생을 모집하고 선생님(한국 명문대 대학원생)을 발굴·매칭하되, 단순 중개가 아니라 하나의 교육 브랜드로 교육 퀄리티와 운영 구조를 자체적으로 관리하는 "비대면 유학 사교육 학원"
- 4개 포털: 학생 / 학부모 / 선생님 / 관리자
- 핵심 차별점: "수업 세션뷰" — 교재(TOC+아티클), 캔버스 필기, 단어장, AI 문제생성, 과제, 연습장(Docs/화이트보드)이 통합된 학생×선생님×회차 귀속 화면

## 레퍼런스 — 기존 목업

`docs/spec/mockups/`에 있는 HTML 파일들이 **완성된 UI/UX 스펙**입니다. 화면을 새로 만들 때는 반드시 먼저 해당 목업 파일을 열어서 구조·데이터 흐름·인터랙션을 확인하세요. 이 목업들은 프로토타입이 아니라 "이대로 만들면 되는" 최종 승인된 디자인입니다.

**`docs/spec/functional-spec.md`를 반드시 먼저 읽으세요.** 목업 파일은 "화면이 어떻게 동작하는지"만 보여줍니다. 이 사업이 무엇이고, 왜 이렇게 설계되었고, 화면들 사이의 관계(특히 "교재"와 "수업 세션"의 레이어 분리)가 무엇인지는 이 문서에 문장으로 설명되어 있습니다. 새 세션을 시작할 때마다 이 문서를 먼저 읽고 작업하세요.

- `alton_landing_v5.html` — 랜딩 페이지
- `alton_auth_v3.html` — 로그인/회원가입
- `alton_admin_portal_v6.html` — 관리자 포털
- `alton_teacher_portal_v12.html` — 선생님 포털
- `alton_student_portal_v16.html` — 학생 포털
- `alton_parent_portal_v9.html` — 학부모 포털
- `alton_material_viewer_prototype.html` — **수업 세션뷰** (가장 중요, 4개 포털에서 전부 진입하는 공통 화면)

## 아키텍처 원칙 (목업에서 확립된 것)

1. **레이어 분리**: "수업 세션"(학생×선생님×회차에 귀속)과 "교재"(관리자가 만든 콘텐츠, 세션과 무관하게 존재)는 명확히 다른 레이어다. 교재 라이브러리에서 교재를 보는 것과, 특정 수업 세션에서 배정된 교재를 보는 것은 다른 화면/다른 데이터여야 한다.
2. **세션뷰는 역할에 따라 같은 화면, 다른 권한**: 학생/선생님이 보는 화면 구조는 같지만 편집 권한(코멘트 수정, 문제 생성 등)이 다르다. URL 파라미터로 흉내낸 부분(`?role=`, `?state=`)은 실제로는 로그인 세션 + DB 관계로 대체되어야 한다.
3. **문제 생성(AI)은 선생님 전용 도구다.** 생성 → 편집 가능한 초안 → 재생성 → "과제로 확정" 절차를 거쳐야 학생에게 노출된다. 학생/학부모에게는 이 기능 자체가 보이지 않아야 한다.
4. **과목 템플릿이 단일 진실 소스(source of truth)**: 관리자가 과목/단원을 과목 템플릿에 추가하면, 교재 생성 폼 등 다른 곳의 선택지가 자동으로 갱신되어야 한다 (하드코딩 목록 금지).

## 기술 스택 (결정됨)

- **프레임워크**: Next.js (App Router), TypeScript
- **DB / Auth / Storage**: Supabase (Postgres)
- **배포**: Vercel
- **스타일**: Tailwind CSS — 목업의 CSS 변수(`--ink`, `--grey-500`, `--red`, `--green` 등)를 그대로 Tailwind 커스텀 테마로 이식
- **테스트**: Vitest (유닛) + Playwright (E2E) — 목업 단계에서 jsdom으로 검증하던 습관을 그대로 이어간다. 기능을 완성했다고 보고하기 전에 반드시 관련 테스트를 실행하고 통과를 확인한다.

## 코딩 컨벤션

- 컴포넌트는 기능 단위 폴더로 (`app/(session)/session-view/`, `app/admin/curriculum/` 등)
- 목업의 한국어 UI 텍스트/라벨은 그대로 유지한다 (번역하지 않음)
- 데이터 모델 필드명은 목업의 변수명을 참고하되, DB 컬럼은 snake_case로 정규화
- 임의로 새 기능을 추가하지 않는다 — 목업에 없는 동작이 필요하면 먼저 사람에게 확인

## 작업 방식

- `docs/tickets.md`에 있는 티켓 목록을 순서대로 진행한다. 한 세션에 티켓 1~3개 정도로 스코프를 제한한다.
- 티켓을 시작하기 전, 관련된 목업 파일을 먼저 읽고 요약한 뒤 작업 계획을 간단히 제시한다.
- 완료 후: (1) 관련 테스트 작성 및 통과 확인 (2) `docs/tickets.md`에서 해당 항목 체크 (3) git commit
- 막히거나 목업 스펙이 불명확하면 추측해서 진행하지 말고 질문한다.
