# 세션 인수인계 — 상담·재상담·복수자녀 온보딩 (2026-09-06)

> **업데이트(2026-09-06, 후속 세션): 이 문서의 "아직 구현되지 않은 것" 절에 있던 복수 자녀 온보딩 핵심 범위가 완료됐다.** 최신 상태는 `docs/CURRENT.md`의 "2026-09-06 복수 자녀 온보딩(최종 확정안, 완료)" 절을 참고 — 기존 단일 칸반 유지, 관리자 학생 1~N명 입력, 보호자 링크 확인 후 학생별 계정·카드 생성, 부분 실패 격리+재시도, 중복 방지가 전부 구현·검증됐다. 아래 본문은 그 작업 착수 시점의 인수인계 기록으로 보존한다(과거 기록 — 새로 읽는 사람은 CURRENT.md를 우선하라).

이 문서는 진행 중이던 대형 UAT+지시 라운드를 다음 세션이 이어받기 위한 인수인계였다. `docs/CURRENT.md`는 이제 이 라운드의 최신 상태로 갱신되어 있다.

## 지금까지 실제로 완료·검증·배포된 것

1. **재상담(기존 보호자 계정) 처리** — 완료, 로컬 검증 완료, non-prod 반영 완료.
   - `find_auth_user_id_by_email(p_email)` — 이메일로 기존 Auth 사용자 존재 여부 확인(service_role 전용).
   - `finalize_trial_onboarding_existing_guardian(p_link_id, p_existing_guardian_id, p_child_auth_user_id)` — 새 household/보호자 profile을 만들지 않고 기존 household에 자녀만 추가. `finalize_trial_onboarding_new_guardian()`과 동일한 신뢰 모델(1회용 온보딩 토큰 교환 성공 = 이메일 접근 확인) 재사용, pending→redeemed 상태 기계 그대로, 재시도 멱등.
   - 마이그레이션: `supabase/migrations/20261128000000_m4_existing_guardian_reconsult.sql`
   - 통합 테스트: `app/consult/existing-guardian-reconsult.integration.test.ts`(5건, 전부 통과)
   - `lib/trial-onboarding-finalize.ts`: `createGuardianAndStudentThenRedirect()` 진입 시 이메일 중복을 먼저 확인해 기존 보호자면 `createStudentAndLinkToExistingGuardianThenRedirect()`로 분기.
   - **왜 필요했나**: 실 UAT 중 같은 이메일로 재상담 시 Auth 계정 생성이 이메일 중복으로 실패해 "보호자 계정 생성에 실패했습니다"만 뜨던 실버그(원인 불명 오류) 재현·수정.
2. **"다른 이메일 사용하기" 버튼 제거** — 완료. `ConfirmEmailForm.tsx`는 이제 읽기전용 이메일 표시 + "이 이메일로 계속" 링크만 제공(관리자가 초대 시 이메일을 이미 기입하므로 편집 불필요, 제품 오너 명시 지시).
3. **재상담 후보(중복 상담) UI 노출** — 완료. 기존에 구현만 되고 안 쓰이던 `find_possible_duplicate_consultations()`를 `ConsultationKanbanBoard.tsx` 상세 모달에 배선(🔁 재상담 후보 박스). 새 데이터 모델 없음, 순수 UI 배선.
4. **상담 카드 정보 노출 보완** — 완료. 카드 목록에 🗓 상담 희망/확정 시각 배지, 📝 상담 리뷰 요약(`admin_review_summary`) 배지 추가. "체험 온보딩" 진입 버튼을 시각적으로 강조(주요 다음 단계로 인지되도록).
5. **로그인 페이지 notice 문구 지원** — 완료. `app/login/page.tsx`가 `?notice=` 쿼리를 에러와 별개로 표시(자녀 추가 연결 완료 안내에 사용).
6. **Smart Notes를 상담 결과 기록(consultation outcome)의 게이트에서 완전히 제거** — 완료(2단계에 걸쳐 실측 확인).
   - `admin_record_consultation_outcome()`은 이제 `consent_confirmed_at` + `admin_review_summary`만 확인한다. `smart_notes_drive_file_id`/`smart_notes_config_status` 조건 둘 다 제거됨(마이그레이션 `20261126000000`/`20261127000000`).
   - 이것으로 **Phase O의 "Smart Notes는 어디서도 게이트가 아니어야 한다" 요구 중, 상담 결과 기록 경로는 검증 완료.** 다른 경로(체험 진행/정규 전환 등)에 Smart Notes 게이트가 남아있는지는 이번 세션에서 전수 재확인하지 못했다 — 아래 "미완료" 참고.
7. **수업 종료 기준 보완(별도의 선행 대형 작업, 이 라운드보다 먼저 완료)** — 완료, non-prod 반영·Preview 재배포 확인 완료. 세부는 이 문서가 아니라 이전 세션 보고 참고(`finalize_lesson_session()` 상태기계 강화, trial/regular 세션 통합, teacher/admin UI 문구 정리 등). 이 라운드와 무관하게 이미 안정 상태.
8. **테스트 회귀 수정** — `lib/trial-onboarding-finalize.test.ts`가 새 `find_auth_user_id_by_email` 선행 호출과 충돌하던 것을 rpcMock을 함수명 기준으로 분기하도록 수정, 재검증 통과.

### 검증 결과(이 커밋 기준)
- `supabase db reset --local` 클린 적용(전체 마이그레이션 포함).
- `npx tsc --noEmit` 클린.
- `npx vitest run` — **169 files / 1135 tests 전부 통과.**
- `npx next build` — 성공.
- `npx supabase db push --linked --dry-run` → `npx supabase db push --linked` — 신규 마이그레이션 1개(`20261128000000`)만 non-prod(`worpsqwqgnspddnrtnvq`, Production 아님)에 적용, `migration list --linked`로 local=remote 전체 일치 재확인.
- 커밋 `0e33076`, 브랜치 `preview/m4-integration-verification`(PR #1)에 push 완료. **main 병합·Production 배포는 하지 않았다.**
- Vercel Preview 자동 재배포는 트리거됐을 것(push 기준)이나 이번 세션에서 Preview 재배포 완료 여부·스모크 테스트는 **재확인하지 않았다** — 다음 세션에서 확인 필요.

## 아직 구현되지 않은 것 (제품 오너가 이번 세션에서 요청한 것 중 남은 부분)

제품 오너의 **최신 확정안(2026-09-06, 이전의 "두 보드 분리" 지시를 대체)**은 다음과 같다.

- 관리자 UI는 **기존 단일 칸반 하나를 유지**한다. 별도 `상담 운영`/`체험·정규 전환 운영` 보드를 만들지 않는다.
- 상담 신청부터 결과 확정 전까지는 보호자/가족 기준의 **상담 건 카드 1개**만 존재한다. 이 시점에는 학생 Auth 계정이나 자녀별 진행 카드를 만들지 않는다.
- 관리자가 기존 `체험 온보딩 안내 발송` 화면에서 보호자 정보와 학생 정보를 입력하는 현재 흐름을 유지한다. 새로운 `체험 대상 자녀 확정` 판단 단계를 만들지 않는다. 학생 입력부만 기본 1행 + `학생 추가` 반복 입력으로 확장해 이름·이메일·학년·과목을 1~N명 개별 입력한다.
- 발송 시점에는 계정을 만들지 않고 보호자에게 온보딩 이메일/링크를 **한 번만** 보낸다. 보호자가 링크를 확인한 뒤 신규 보호자는 보호자 Auth/household를 한 번 만들고, 기존 보호자는 기존 Auth/household를 재사용한다. 시스템이 입력된 N명의 학생 Auth 계정을 만들고 같은 household에 연결하며, 학생별 비밀번호 설정 초대를 각각 보낸다.
- 관리자에게 보이는 자녀별 진행 카드는 각 학생 계정/온보딩 단위가 실제로 생성된 뒤 같은 칸반에 각각 만든다. 계정 생성 전부터 보이는 임시 자녀 카드를 별도 제품 상태로 만들 필요는 없다. 일부 학생 생성이 실패하면 성공한 형제자매를 롤백하지 않고 실패한 학생만 재시도할 수 있어야 한다.
- 원 상담 카드는 상담 완료 이력으로 보존하고, 이후 활성 칸반에는 생성된 자녀별 카드가 체험 준비→체험→리뷰→정규 전환 단계를 독립적으로 이동한다.
- 상담 공통 일정·Calendar/Meet·상담 동의·상담 Smart Notes·관리자 상담 요약은 원 상담 건에 한 번만 둔다. 학생 초대·체험 동의·체험수업권·배정·예약·리뷰·정규 전환만 자녀별로 관리한다.
- 자녀 카드는 가족ID/householdID와 원 상담건ID로 연결하고 가족 배지·필터로 형제자매를 함께 찾을 수 있게 한다. 동일 상담·동일 자녀 온보딩의 중복 카드/초대/수업권 생성을 DB 제약과 멱등성으로 막는다.
- 단일 칸반에서 상담 구간과 자녀별 진행 구간은 구분선 또는 섹션 라벨만 둔다. 보드 자체를 둘로 나누지 않는다.

**이번 세션에서 위 최신 카드 분기 구조는 구현하지 못했다.** 현재 구현은 관리자 입력 1명→보호자 링크 1개→보호자 확인 후 학생 계정 1명 생성만 전제한다. 핵심 변경은 이 기존 흐름의 학생 입력/생성 부분을 1~N명으로 확장하고, **성공적으로 생성된 학생별로 기존 단일 칸반에 진행 카드를 각각 만드는 것**이다.

### 현재 계정 생성 주체와 이번/후속 범위

- 현재 M4 기본 경로에서 관리자는 보호자·학생 정보를 입력하고 온보딩 발송을 시작할 뿐 Auth 계정을 직접 생성하지 않는다. 보호자도 Auth 계정을 수동 생성하지 않는다. 보호자가 이메일 링크를 확인하면 시스템이 신규 보호자/학생 계정을 자동 생성하거나 기존 보호자 계정을 재사용한다.
- 보호자 포털 `가족` 탭의 기존 자녀 직접 초대는 별도의 R2 경로이며, 상담→체험 온보딩 기본 경로와 혼동하지 않는다.
- **이번 라운드**에는 관리자가 학생 1~N명의 이메일을 개별 입력한다. 한 자녀일 때의 기존 동작은 그대로 유지한다.
- **후속 백로그**에는 보호자가 온보딩 링크에서 학생 수와 자녀별 이름·이메일·학년·과목을 직접 입력/확인하는 방식으로 전환하는 UX를 등록한다. 이번 라운드에서 앞당기지 않되, 데이터 모델은 나중에 입력 주체만 바꿀 수 있도록 관리자 입력값을 불변의 최종 원본으로 과도하게 결합하지 않는다.

### 상담과 재원생 면담의 UI 경계(최신 확정)

- 랜딩의 상담은 신규 보호자의 가입 상담이다. 기존 보호자는 보호자 포털의 `자녀 추가 상담`에서 새 자녀 온보딩 상담을 신청한다.
- 보호자 포털의 현재 `가족` 탭에 있는 상담 없는 직접 학생 초대는 기본 흐름에서 제거하고 `새 자녀 상담 신청`으로 연결한다. 기존 초대 로직은 상담 후 확정된 자녀 온보딩에서 재사용한다.
- 랜딩과 보호자 포털의 자녀 추가 상담은 학생 수업 예약과 같은 월간 캘린더→날짜→시간 선택 UI를 공통 컴포넌트로 사용한다. 제출 권한/연결만 비로그인 prospect와 인증 guardian으로 분리한다.
- 기존 자녀의 학습·일정·선생님·서비스 현황 논의는 가입용 `상담`과 섞지 않는다. R11 보호자–관리자 메신저에서 비동기 `문의`를 처리하고, 실시간 대화가 필요할 때 `면담 일정 요청`으로 전환한다. 관리자는 향후 별도 `면담 운영`에서 관리한다.
- 면담도 같은 캘린더 UI를 재사용하되 데이터·상태·운영 화면은 가입 상담과 분리한다. 면담 시간, 별도 가용시간, Smart Notes/동의 정책은 R11 착수 시 확정하며 이번 라운드에는 가짜 면담 UI를 만들지 않는다.

### 다음 세션이 먼저 해야 할 일 (제품 오너 지시 순서 그대로)

제품 오너의 마지막 지시("앞서 보낸 두 지시는 하나의 통합 작업 라운드로 처리")는 **먼저 3분류(이미구현/부분구현/신규구현 필요)를 내부적으로 분류한 뒤 진행**하라고 명시했다. 이번 세션은 이 분류를 문서화된 형태로 보고하지 못한 채 시간이 부족해 인수인계로 전환했다. 다음 세션 시작 시:

1. 이 문서 + 제품 오너의 원문 세 메시지(세션 로그 또는 이전 요약 참고)를 함께 읽고, 아래 표를 최신화한다.
2. 기존 `체험 온보딩 안내 발송`의 학생 입력을 1~N 반복행으로 확장하고, 보호자 링크 확인 시 N명 계정을 생성하는 경계를 기준으로 실제 마이그레이션 범위를 정한다. 상담 신청 단계에서는 학생 계정이나 자녀 진행 카드를 미리 만들지 않는다.
3. 한 보호자 링크 아래 자녀별 생성 상태·초대 상태·실패 격리·중복 방지를 책임지는 구조를 둔다. 성공한 학생은 유지하고 실패한 학생만 재시도하며, 같은 학생의 중복 Auth/카드/수업권을 만들지 않는다. 칸반 stage는 기존 `trial_sessions`/entitlement/배정/예약/리뷰/계약 상태에서 가능한 한 파생한다.
4. 계정 생성에 성공한 학생별로 단일 칸반 카드를 만든 뒤 자녀별 초대 발송, 체험 동의, entitlement, 리뷰 독립성 및 Smart Notes 비게이트 검증을 연결한다.

### 아직 손대지 못한 나머지 항목(원 지시 ①의 12항목 기준, 우선순위 참고용)

- 기존 관리자 온보딩 발송 폼의 복수 학생 입력(기본 1행 + 학생 추가, 학생별 이름/학년/과목/이메일)과 보호자 링크 확인 후 N명 계정 생성.
- 보호자 포털 `자녀 추가 상담` 및 진행 중/지난 상담 UI, 현재 가족 탭의 직접 초대 진입점 교체.
- 랜딩과 보호자 포털 공통 상담 캘린더(월간 날짜→시간 선택) UI.
- 자녀별 독립 초대 발송(현재는 온보딩 링크가 상담 1건당 사실상 1개 자녀만 가정).
- 상담-레벨(회의 참석 전체) vs 체험-레벨(자녀별) Smart Notes 동의 분리 저장 — 현재는 계약 조항 기반 단일 동의 모델(위 6번 항목)이라 이 세분화가 아직 없음. **단, "Smart Notes가 어디서도 게이트가 아니어야 한다"는 요구 자체는 상담 결과 기록 경로에서는 충족됨(위 6번).** 체험 진행/정규 전환 등 다른 경로에 남은 게이트가 있는지 `grep -rn "smart_notes" app/ supabase/migrations/` 로 전수 재확인 필요 — 이번 세션에서 못함.
- 자녀별 entitlement 완전 독립(1건 실패가 형제 자매에 영향 주면 안 됨) — 기존 R4 entitlement 모델이 이미 자녀(child_id) 단위이므로 구조적으로는 이미 독립적일 가능성이 높으나, 명시적으로 검증하는 테스트는 없다.
- 즉시 리뷰 작성 + 나중 도착하는 AI 요약(`lesson_reviews.ai_summary`)이 사람이 작성한 리뷰를 덮어쓰지 않는지 — 컬럼은 이미 있고 게시된 리뷰를 자동 갱신하는 코드 경로 자체가 없으므로 구조적으로는 이미 안전하지만, 이것도 명시적 테스트는 없다.
- 비prod 데이터 정리 — 제품 오너가 `official@alton.education`과 `teacher1@alton.education` 및 필수 운영 데이터만 보존하고 나머지 UAT 고객 데이터를 정리하도록 **명시 승인 완료**. ID allowlist 사전 확인, dry-run, immutable 감사이력 비우회, schema/migration/env/webhook 보존 조건으로 진행한다. 재확인 질문은 불필요하다.

## 명시적으로 하지 않은 것(제약 준수 확인)

- main 병합 없음, Production 배포 없음, 실제 Google/DocuSign/Stripe 호출 없음, 실제 고객 대상 발송 없음.
- 전체 외부 골든패스 UAT 재실행 없음(제품 오너가 직접 하기로 함).
- 비prod 데이터 정리는 이전 세션에서는 실행하지 않았으나 이후 제품 오너가 위 안전 범위로 명시 승인했다.

## 다음 세션 시작 체크리스트

1. `CLAUDE.md` → 이 문서(`docs/2026-09-06-session-handoff-consult-multichild.md`) → 필요 시 `docs/CURRENT.md` 최신 섹션 순으로 읽는다.
2. 이 문서의 최신 확정안(단일 칸반 + 기존 관리자 온보딩 폼의 복수 학생 입력 + 보호자 링크 확인 후 학생별 계정/카드 생성)을 기준으로 기존 구현을 이미 구현/부분 구현/신규 구현으로 분류한다. 폐기된 두 보드 분리안과 A/B 후보안을 다시 검토하지 않는다.
3. Vercel Preview가 최신 커밋(`0e33076`)으로 재배포됐는지, 기본적인 재상담 흐름이 Preview에서 동작하는지 가벼운 스모크 확인부터 한다(전체 골든패스 재실행은 아님).
4. 이후 위 "다음 세션이 먼저 해야 할 일" 순서대로 비prod 정리 → 관리자 온보딩 폼/계정 생성의 복수 학생 확장 → 단일 칸반 카드 분기/UI → 테스트 → non-prod 반영 순으로 진행한다.
