# SAT Math 파일럿 배치 실행 로그

실행: 2026-09-16T17:04:14.547Z

## linear_functions (Algebra (선형식·연립·부등식))
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 3.4

## linear_equations_two_var (Algebra (선형식·연립·부등식))
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 8.6
- 실패 사유: 점 A 라벨 '(-4, -1)' 가 선과 겹칩니다. / 점 B 라벨 '(2, 5)' 가 선과 겹칩니다. / 독립 검사는 B)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 독립 검사는 B)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 독립 검사는 C)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 점 Q 라벨 '(5, 140)' 가 선과 겹칩니다. / 선택지 1에 닫히지 않은 수식 기호 $ 가 있습니다.

## linear_inequalities (Algebra (선형식·연립·부등식))
- 요청 5 / 파이프라인 통과 3 / 오답 보강 대기 0 / 저장 완료(초안) 3 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 9.33
- 실패 사유: 점 A 라벨 '(0, -2)' 가 선과 겹칩니다. / 지문에 배치 용어(region, northeast 등)가 있습니다 — 좌표·점 이름·직선 이름으로 부릅니다. / 독립 검사 지적: 질문 자체에 정답 부등식(y ≥ 1/2 x - 2)이 그대로 노출되어 있어 사실상 대조만 하면 풀리는 문제 / 독립 검사는 B)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 지문에 방위·영역 표현이 있습니다 — 점·변·각 이름으로 부릅니다.

## equivalent_expressions (Advanced Math (이차·지수·다항))
- 요청 5 / 파이프라인 통과 3 / 오답 보강 대기 0 / 저장 완료(초안) 3 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 17.67
- 실패 사유: 독립 검사는 B)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 정답-해설 불일치: 해설은 D를 뒷받침하지만 지정 정답은 A입니다(Math는 정답을 자동으로 바꾸지 않습니다). / 독립 검사는 B)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 독립 검사는 B)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 독립 검사는 C)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음)

## nonlinear_functions (Advanced Math (이차·지수·다항))
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 4.4
- 실패 사유: 점 A 라벨 '(-4, 0)' 가 선과 겹칩니다. / 점 B 라벨 '(2, 0)' 가 선과 겹칩니다. / 정답-해설 불일치: 해설은 C를 뒷받침하지만 지정 정답은 B입니다(Math는 정답을 자동으로 바꾸지 않습니다). / 점 A 라벨 '(0, 3)' 가 선과 겹칩니다. / 점 B 라벨 '(2, 12)' 가 선과 겹칩니다. / 독립 검사는 C)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 독립 검사 지적: 문제 자료 설명에 g(x)=3*2^x로 이미 공식이 주어져 있어 실제로는 계산만 하면 되므로 난이도가 낮아질 수 있음(단, 이는 자료 설명일 뿐 학생에게 직접 노출되는 것은 아님)

## ratios_rates_units (Problem-Solving and Data Analysis)
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 5.4
- 실패 사유: 지문은 'grams' 단위를 쓰지만 표·그래프의 열 이름이나 축 제목에 단위가 없습니다(예: 'Cost (dollars)'). / 정답-해설 불일치: 해설은 B를 뒷받침하지만 지정 정답은 C입니다(Math는 정답을 자동으로 바꾸지 않습니다). / 독립 검사는 C)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 독립 검사는 C)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음)

## percentages (Problem-Solving and Data Analysis)
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 9.4
- 실패 사유: 범주 이름 'Ages 55 and older' 가 너무 길어 축 아래에 놓을 수 없습니다 — 짧게 줄이세요. / 지문의 'results are shown in the bar' 에 해당하는 항목이 데이터에 없습니다. / 지문의 'results are shown in the bar' 에 해당하는 항목이 데이터에 없습니다. / 지문은 '18-34'(Support) 값을 250 로 말하지만 그래프의 값은 60 입니다. / 지문에 닫히지 않은 수식 기호 $ 가 있습니다. / 선택지 1에 닫히지 않은 수식 기호 $ 가 있습니다. / 지문에 닫히지 않은 수식 기호 $ 가 있습니다. / 해설에 수식 기호($…$) 밖에 LaTeX 제어문이 있습니다 — 그대로 노출됩니다: "\times" / 지문의 수치가 표·그래프 자료의 값과 하나도 겹치지 않습니다 — 자료가 지문의 근거가 아닙니다.

## two_variable_data (Problem-Solving and Data Analysis)
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 2.8

## probability (Problem-Solving and Data Analysis)
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 3.2

## inference_margin_error (Problem-Solving and Data Analysis)
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 3.2
- 실패 사유: 지문은 'hours' 단위를 쓰지만 표·그래프의 열 이름이나 축 제목에 단위가 없습니다(예: 'Cost (dollars)'). / 지문은 'hours' 단위를 쓰지만 표·그래프의 열 이름이나 축 제목에 단위가 없습니다(예: 'Cost (dollars)').

## evaluating_statistical_claims (Problem-Solving and Data Analysis)
- 요청 5 / 파이프라인 통과 5 / 오답 보강 대기 0 / 저장 완료(초안) 5 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 5.4

## area_volume (Geometry and Trigonometry)
- 요청 5 / 파이프라인 통과 4 / 오답 보강 대기 0 / 저장 완료(초안) 4 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 15
- 실패 사유: 지문은 높이 4 인데 도식 라벨은 '10' 입니다. / 지문은 높이 4 인데 도식 라벨은 '10' 입니다. / 중심각 라벨(AB) '90°' 가 'S' 와 겹칩니다. / 중심각 라벨(AB) '90°' 가 '8' 와 겹칩니다. / 중심각 라벨(AB) '90°' 가 'S' 와 겹칩니다. / 중심각 라벨(AB) '90°' 가 '8' 와 겹칩니다. / 지문의 변 AB 가 도형에 없습니다. / 지문의 변 BC 가 도형에 없습니다.

## right_triangles_trigonometry (Geometry and Trigonometry)
- 요청 5 / 파이프라인 통과 4 / 오답 보강 대기 0 / 저장 완료(초안) 4 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 11.75
- 실패 사유: 지문의 각 '90°' 가 도형의 각 라벨에 없습니다. / 지문의 각 '180°' 가 도형의 각 라벨에 없습니다. / 지문의 각 '90°' 가 도형의 각 라벨에 없습니다. / 정답-해설 불일치: 해설은 C를 뒷받침하지만 지정 정답은 B입니다(Math는 정답을 자동으로 바꾸지 않습니다). / 독립 검사는 B)을 정답으로 골랐습니다 — 지정 정답과 다릅니다(정답이 둘이거나 지정 정답이 틀렸을 수 있음) / 현 라벨 'AB' 을 겹치지 않게 놓을 자리가 없습니다.

## circles (Geometry and Trigonometry)
- 요청 5 / 파이프라인 통과 4 / 오답 보강 대기 0 / 저장 완료(초안) 4 / 저장 시점 거부 0
- 평균 모델 호출 수(파이프라인 stats): 8.25
- 실패 사유: 중심각 라벨(AB) '40°' 가 '9' 와 겹칩니다. / 중심각 라벨(AB) '60°' 가 '6' 와 겹칩니다. / 지문은 OS = 13 인데 그림에 그 길이 라벨이 없습니다. / 점 이름(P) 'P' 가 'PA' 와 겹칩니다. / 접선 라벨 'PB' 가 'P' 와 겹칩니다. / 중심각 라벨(AB) '90°' 가 'S' 와 겹칩니다. / 중심각 라벨(AB) '90°' 가 '8' 와 겹칩니다.


## 전체 결과
- 요청 70문항 / 저장 완료(초안) 63문항 (통과율 90%)
- 총 API 호출 384회, 입력 토큰 927,865, 출력 토큰 227,874
- 예상 비용: 입력 $1.8557 + 출력 $2.2787 = 총 $4.1345 (Claude Sonnet 5 $2/$10 per MTok 기준)
- 문항당 평균 비용: $0.0656
- **저장은 초안(draft)까지만 — 공개(published)는 관리자가 admin 문제은행에서 직접 확인 후 눌러야 한다.**
