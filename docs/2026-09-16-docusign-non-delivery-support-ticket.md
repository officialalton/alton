# DocuSign Support 문의 자료 — 초대 이메일 전달 여부 확인 요청

## 요약
동일 계정(Alton Education, account id 919e8493-2043-48e4-89c6-fc8c91cf7c24, demo/sandbox)·
동일 발송자(official@alton.education)·**동일 수신 주소**(matchbox512+alton-uat-p13@gmail.com)로
보낸 3개 envelope를 비교한 결과, 실제로 수신함에 도착이 확인된 것과 안 된 것의 유일한 구조적
차이는 **envelope를 생성한 클라이언트(발신 IP)** 다:
- 도착 안 됨(Gmail에서 제목으로 검색해도 없음, 스팸함에도 없음) — 생성 IP `35.162.238.117`
  (자사 백엔드 서버의 고정 IP, OAuth JWT 서비스 계정 통합으로 생성)
- 도착 확인됨(받은편지함에서 육안 확인) — 생성 IP `2600:1702:7bd0:a090:e16f:5015:2041:ce85`
  (담당자 본인이 브라우저로 DocuSign에 로그인한 세션의 IP)

**참고**: `Source: api`라는 필드값 자체는 두 경우 모두 동일하게 찍힌다(DocuSign 웹 UI도
내부적으로 같은 REST API를 호출하는 구조로 보이므로 `Source` 필드만으로는 "누가 만들었나"를
구분할 수 없다 — 대신 `ClientIPAddress`가 실제 구분 기준이다). 또한 `Delivered` 상태는
수신자가 봉투를 실제로 열람했을 때만 기록되는 것으로 보이며 이메일이 수신함에 도착했는지
자체를 직접 나타내지 않는다 — 그래서 이 문의는 "이메일이 전달 안 됐다"를 상태값이 아니라
Gmail에서 실제로 검색해 확인한 결과를 근거로 한다.

## 대상 Envelope(같은 계정·같은 발송자·같은 수신 주소)
- 도착 안 됨(자사 서버 IP 35.162.238.117에서 생성): `e5642671-ad51-8342-8015-b1a3cd9710f5`
  (2026-09-16 16:09:56 UTC 발송, 17:19:32 UTC 재발송 — 둘 다 도착 안 됨),
  `e8c3213e-2e8d-8cb6-8069-62c2559f10e8`(2026-09-16 16:07:48 UTC 발송, 도착 안 됨)
- 도착 확인됨(담당자 브라우저 세션 IP 2600:1702:...에서 생성): `7e702930-ee59-8ffe-8092-20443797107b`
  (2026-09-16 17:18:55 UTC 발송, 받은편지함에서 실제 확인)

## 확인 요청 사항(DocuSign Support)
1. 위 세 envelope의 실제 이메일 dispatch 로그를 비교해 달라 — 자사 서버 IP(35.162.238.117,
   JWT 서비스 계정 통합)로 생성한 두 건과, 담당자 본인 브라우저 세션으로 생성한 한 건 사이에
   실제 발송 시도·반송(bounce)·억제(suppression)·지연(deferral) 처리에 차이가 있는지.
2. 이 계정의 통합 키(Integration Key)/서비스 계정으로 생성한 envelope에 대해서만 적용되는
   이메일 발송 제한·정책(예: 미검증 통합 키에 대한 발송 제한, 도메인 검증 요구 등)이 있는지.
3. `Delivered` 상태가 "수신자가 실제로 열람했을 때"만 기록되는 것이 맞는지, 그렇다면 이메일이
   실제로 수신함에 도착했는지 여부를 API/UI 어디에서 확인할 수 있는지.

## 확인된 것(추가 조사로 배제한 가설)
- 계정 플랜/과금: Developer/Demo, envelope 13/Unlimited — 쿼터 문제 아님.
- 수신자가 계정 내부 사용자(member)인지: 계정 전체 사용자 1명(Do Kyung Kim)뿐 — 수신자는
  계정 멤버 아님. recipient 객체의 userId 필드는 비회원 수신자에게도 DocuSign이 부여하는
  내부 식별자(DocuSign 개발자 지원 확인)이며 계정 멤버십을 의미하지 않음.
- Gmail plus-addressing(`+alton-uat-p13`)이 원인이라는 가설: 배제 — 정확히 같은 주소로 보낸
  envelope(`7e702930...`)가 실제로 도착했다(담당자 본인 세션에서 생성한 건). 주소 형식은
  변수가 아니다.
- 수신 도메인(Gmail vs 기관 도메인) 차이가 원인이라는 가설: 배제 — 위와 같은 이유.
- 발송 요청 자체의 오류: 없음(API가 200/201 정상 응답, envelopeId 정상 발급, status="sent" 정상 반영).

## 추가 조치 금지
원인 확인 전까지 이 envelope들에 대한 추가 재발송·신규 계약 생성·설정 변경은 하지 않음.
