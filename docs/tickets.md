# Alton Education — 개발 티켓 목록

우선순위: **고객(학생/학부모)이 실제로 보는 화면**을 먼저 실물로 만들고, 관리자 도구는 "굴러가는 수준"으로 먼저 만든 뒤 나중에 다듬는다.

각 티켓은 Claude Code 세션 1회에 1~3개씩 처리하는 것을 기준으로 잘라놓았다. 진행하면서 체크(`[x]`)하고, 세션 끝날 때 이 파일도 함께 커밋한다.

---

## Phase 0 — 기반 (건너뛸 수 없음)

- [x] **000-project-init**: Next.js + TypeScript + Tailwind + Supabase 프로젝트 초기화, Vercel 연결, 목업 CSS 변수 → Tailwind 테마 이식 (2026-08-28: 남아 있던 "Vercel 연결"까지 완료. 새 Supabase 프로젝트(`worpsqwqgnspddnrtnvq`)로 이관 + 전체 마이그레이션 push, GitHub `officialalton/alton`으로 히스토리 이관(리베이스), Vercel 프로젝트(`alton7/alton`)를 그 GitHub 저장소에 연결해 `main` 푸시 시 자동 배포되도록 구성 — 프로덕션 [alton-ecru.vercel.app](https://alton-ecru.vercel.app) 라이브)
- [x] **001-schema-design**: 목업 파일 전체를 검토해 DB 스키마 초안 작성 (사람 검토 완료, `docs/spec/schema-draft.md` 참고)
- [x] **002-schema-migrate**: 001에서 승인된 스키마로 Supabase 마이그레이션 작성 + 시드 데이터 (Docker 로컬 검증 완료, 호스팅된 프로젝트에도 push 완료. 개발용 시드 데이터는 로컬 전용이라 실제 프로젝트엔 안 넣음)
- [x] **003-auth-roles**: 로그인(학생/학부모/선생님/관리자), 역할 기반 라우팅. 목업의 `?role=` URL 파라미터 흉내를 실제 세션 기반으로 대체 (로컬 Supabase로 로그인/역할 리다이렉트/미들웨어 차단/실제 이메일 비밀번호 재설정까지 전부 브라우저로 실제 테스트함)

## Phase 1 — 수업 세션뷰 (가장 중요, 고객이 매번 보는 화면)

- [x] **010-session-shell**: 세션뷰 레이아웃, 상단바(세션 상태바: 준비중/진행중/완료), 역할별 권한 분기 — `alton_material_viewer_prototype.html` 참조 (목업 대비 개선: 탭을 `?tab=` URL로 관리해 새로고침/공유 가능하게 함, 상태를 30초마다 재계산해 자동 전환, 뷰어를 student/teacher/parent/admin 4종으로 확장, 로컬 DB 실데이터로 prep/live/completed 전부 검증)
- [x] **011-session-material-tab**: 교재 탭 — TOC 네비게이션(IntersectionObserver 스크롤스파이) + 본문/티칭팁(선생님 전용, 토글) 렌더링 + 문제 풀이(객관식 3회 채점/서술형/화이트보드) 실제 DB 연동. mock 대신 실제 `curriculum_docs`/`problems`/`session_problem_attempts` 사용 — "선생님 픽"·⭐저장은 017로 넘김. 학생 실제 풀이→새로고침 후 상태 유지, 선생님 뷰(정답 배지+티칭팁) 전부 브라우저로 검증
- [x] **012-session-canvas**: 캔버스 필기 (펜/색상/지우기, 스크롤 콘텐츠 위 오버레이) — Supabase Realtime Broadcast로 실시간 동기화, `canvas_annotations`에 영속 저장. 서로 다른 브라우저 연결 2개로 실시간 전파(그리기/전체지우기) + 새로고침 후 DB에서 복원 전부 실제 검증
- [x] **013-session-vocab**: 단어장 — 단어 클릭 선택 + AI 뜻풀이 생성, 학생 포털의 "단어장" 탭과 데이터 공유 (060 대기 없이 바로 Claude API(Haiku 4.5) 연동. 학생/선생님 둘 다 단어 추가 가능, 삭제는 학생 전용으로 RLS까지 맞춰 확정 — 실제 클릭→AI 생성→저장→삭제 전부 브라우저로 검증)
- [x] **014-session-homework**: 과제 탭 — 학생 인라인 답안 작성(블러 시 자동 저장), 선생님 과제 추가(네이티브 prompt() 대신 인라인 폼) — 실제 DB로 교사 추가→학생 답안 작성→교사 확인 전체 플로우 브라우저로 검증. insert RLS도 선생님/관리자 전용으로 조여둠
- [x] **015-session-aigen**: 문제 생성 (선생님 전용) — 조건 선택 → AI 생성 → 편집 가능한 초안 → 재생성 → "과제로 확정" (060 대기 없이 바로 Claude API(Sonnet 5) 연동, tool-use로 구조화된 문제 배열 생성. 목업 대비 개선: 과목은 세션에 이미 귀속되어 있으므로 선택 UI 생략, "문제 유형"은 과목마다 하드코딩된 매핑 대신 자유 텍스트로 변경(스키마의 skill_type이 자유 텍스트인 것과 일치), 단원 목록은 `subject_template_units`에서 실시간 로드(하드코딩 금지 원칙). 초안은 클라이언트 상태로만 유지하다가 확정 시 `problems`(status='confirmed')+`homework_items`를 함께 insert. 확정 즉시 새로고침 없이 과제 탭에 반영되도록 SessionShell에서 과제 목록 상태를 끌어올림(lift). 실제 브라우저로 생성→편집→재생성→삭제→확정→학생 화면 노출까지 전체 플로우 검증, "문제 생성" 탭이 학생에게 전혀 안 보이는 것도 확인. 검증 중 두 가지 실제 버그 발견/수정: (1) `problems` insert RLS가 `created_by=auth.uid()`만 확인해 아무나 임의 세션에 문제를 끼워넣을 수 있었음 → 세션 담당 선생님/관리자로 제한하는 마이그레이션 추가, (2) 서버 액션(`generateProblems`/`finalizeProblemsToHomework`)에 호출자가 해당 세션 선생님인지 확인하는 가드가 없었음 → 추가. 그 외 재생성이 초안 생성 당시가 아닌 폼의 현재 단원을 사용하던 버그도 수정(초안별로 단원을 저장하도록 변경))
- [x] **016-session-scratchpad**: 연습장 — Docs/화이트보드 서브탭, 화이트보드 스크롤 가능 (schema-draft.md에 tldraw안과 Supabase Realtime안이 상충되게 남아있던 걸 발견 → 사람 확인 후 Realtime으로 확정, 012 캔버스 필기와 동일한 패턴 재사용. Docs 서브탭은 목업이 정적 링크 목록뿐이라 선생님용 추가/삭제 인라인 폼을 새로 만듦(하드코딩 목록은 실서비스에 안 맞음). 화이트보드는 학생/선생님 둘 다 그릴 수 있는 공유 캔버스로 구현, `sessions.whiteboard_strokes`에 영속 저장. 검증 중 session_doc_links 쓰기 RLS가 라벨과 달리 학생도 허용하던 버그 발견 → 선생님/관리자 전용으로 수정(homework_items 때와 동일 패턴). 실제 브라우저로 선생님 문서 추가/삭제, 화이트보드 필기→새로고침 복원, 학생 계정으로 전환해 공동 필기 및 문서 추가/삭제 버튼 미노출까지 검증)
- [x] **017-session-problemlog**: 문제 기록 — 풀이 이력, 필터(과목/단원/정답여부/저장) (목업 대비 개선: 필터를 단일 선택 라디오 목록 대신 독립적인 차원(과목/단원/정답여부/형식/저장/선생님픽)으로 분리해 동시 조합 가능하게 함, 과목/단원 필터는 학생이 실제로 푼 기록에서 동적으로 추출(과목이 1개뿐이면 숨김). 스키마 보완: AI 생성 문제도 단원 필터를 탈 수 있도록 `problems.unit_title` 비정규화 컬럼 추가(015 아이겐 플로우도 함께 갱신), 목업의 "선생님 픽 사유 복수선택"을 지원하도록 `teacher_problem_tags.reason`을 단일 enum→배열로 변경. "다시 풀기"는 세션과 무관한 연습 시도로 취급해 `session_id=null`로 새 3회 채점 사이클 부여. 실제 브라우저로 학생 계정 풀이 기록 생성→필터링→저장 토글→다시 풀기→선생님 계정 전환 후 선생님 픽 다중 태깅→다시 학생 계정으로 배지 노출 확인까지 전체 플로우 검증. 검증 중 teacher_problem_tags RLS 2건 발견/수정: select 정책이 태깅한 선생님만 조회 가능해 정작 학생 본인이 자기 문제의 선생님 픽 여부를 못 봤음, insert/update 정책도 가르치는 학생인지 확인 없이 아무 선생님이나 태깅 가능했음)

## Phase 2 — 학생 포털

- [x] **020-student-shell**: 사이드바, 홈 대시보드 (목업 사이드바 9개 항목(홈/레슨/선생님/과제/문제/단어장/교재/수업권/통계) 전부 배치하되, 이번 티켓 스코프인 "홈"만 실제 구현하고 나머지는 각자 티켓에서 채움. 홈은 캘린더(이번 달, 수업 있는 날 점 표시+클릭 상세)+예정된 수업(최대 3개, 클릭 시 세션뷰로 이동)+통계 요약(참여율)으로 구성 — 목업 그대로. `?tab=` URL 동기화로 새로고침/공유 가능하게 함(세션뷰와 동일 패턴). 여러 enrollment(여러 과목/선생님)를 가진 학생도 캘린더/예정수업에 전부 합산되도록 구현. 실제 브라우저로 학생 계정 로그인→캘린더/예정수업/통계 실데이터 확인, 세션뷰 이동, 로그아웃까지 검증하며 선생님 이름 중복 표시 버그("OO 선생님 선생님") 발견/수정 — `profiles.name`에 이미 "선생님" 호칭이 포함되어 있었음). **주의**: 목업 사이드바의 "과제"/"문제"/"단어장"/"통계" 4개 항목은 tickets.md에 별도 티켓이 없다 — "문제"/"단어장"은 017/013에서 이미 만든, 세션에 종속되지 않는 컴포넌트(`ProblemLogTab`/`VocabTab`)를 거의 그대로 재사용할 수 있어 보이고, "과제"/"통계"는 새로 설계가 필요하다. 025번대 티켓으로 추가할지 확인 필요.
- [x] **021-student-lessons**: 레슨(예정/지난 수업), 커리큘럼 뷰 (목업 대비 스코프 조정: 세션뷰가 실시간 상태를 자동 계산하므로 "수업 준비"/"수업 진행" 두 버튼을 하나로 통합(계산된 상태에 따라 라벨만 전환), "지각 알림"/"변경·취소"는 알림·예약 인프라(Phase 7)가 없어 이번 티켓에서 제외. 커리큘럼 뷰는 `teacher_curriculum_template_units`를 세션과 조인해 회차별 완료/진행중/예정 상태를 계산, 완료 회차엔 "리뷰 보기"(session_reviews 읽기 전용 표시 + 학생 별점·코멘트 피드백 제출) 연결, 메모·피드백 카드(session_memos)도 목업대로 포함. 실제 브라우저로 예정/지난 수업 목록, 커리큘럼 진행 상태, 리뷰 별점 제출→새로고침 후 유지, 메모 추가까지 검증. 검증 중 실제 RLS 버그 발견/수정: `teacher_curriculum_templates`/`teacher_curriculum_template_units`의 select 정책이 선생님 전용이라 정작 그 커리큘럼을 배정받은 학생 본인이 자기 진도를 전혀 못 봤음 → 활성 enrollment가 있는 학생에게 조회 권한 추가)
- [x] **022-student-teacher-tab**: 선생님 탭(별도 최상위), 프로필, 메시지 (선생님 목록(과목·회차 진행도 칩)→프로필(학교/자기소개/수업 내역)→메시지 순 서브뷰. 메신저는 목업이 학생당 담당 선생님 첫 번째로 고정된 1:1이었지만, 실제로는 선생님별로 카드에서 "💬 메시지"를 눌러 각자의 스레드(`chat_threads`, student+teacher 유일)로 들어가도록 개선(다과목/다선생님도 자연스럽게 지원). `chat_messages`는 Supabase Realtime(`postgres_changes` on INSERT)으로 실시간 수신. 실제 브라우저로 프로필/수업내역 확인, 메시지 전송→실제 DB 저장→새로고침 후 유지까지 검증. 검증 전 실제 버그 2건 발견/수정: (1) `chat_threads`/`chat_messages`의 select RLS에 학부모 조회 권한이 아예 빠져있었음(목업 카피 "학부모님과 관리자가 항상 열람 가능"과 실제 권한이 어긋남) → `is_guardian_of` 추가, (2) 시드 데이터에서 이도현 선생님의 `school` 값이 최지우 선생님 것과 뒤바뀌어 있었음(목업 텍스트 기준으로 바로잡고 `bio`도 채움))
- [x] **023-student-materials-library**: 교재 탭 — 수강 과목 한정, **레이어 분리 주의**(세션뷰 아님, 순수 교재 뷰) (목업이 실제로 새 탭(`?mode=library`)에서 여는 동작이었던 걸 그대로 살려, 세션뷰와 완전히 독립된 최상위 라우트 `/materials/[id]`로 구현 — StudentShell 탭 안에 끼워넣지 않음. 목록(`교재` 탭)은 수강 중인 과목만, 상세(`/materials/[id]`)는 목차+본문만 보여주고 과제/문제생성/문제기록/단어장/연습장 등 세션 귀속 기능은 전혀 노출 안 함(functional-spec 레이어 분리 원칙 그대로). 임베디드 문제는 스펙에 명시 안 됐지만 세션 없이는 시도 기록이 불가능하므로 정답·해설이 바로 보이는 읽기 전용 프리뷰로 구현. 실제 브라우저로 과목별 그룹핑, 라이브러리 진입 시 세션 chrome 전혀 없는 것, 기존 세션뷰 교재 탭 회귀 없음, 존재하지 않는 교재 접근 시 안내문구까지 검증. 검증 전 실제 RLS 버그 발견/수정: `curriculum_docs`/`curriculum_doc_sections`의 select 정책이 "배포됨"만 확인해서 로그인만 하면 수강 여부와 무관하게 전체 과목의 모든 교재를 열람할 수 있었음 → 해당 과목으로 enrollment가 있었던 학생/선생님/보호자만 조회 가능하도록 조임(과거 세션 자료 열람이 안 끊기도록 status 조건은 안 걸음). 시드에 교재 1개를 추가로 넣어 라이브러리 그룹핑을 실제로 검증할 수 있게 함)
- [x] **024-student-credits**: 수업권 현황 (목업 그대로 잔여 수업권(`students.credit_balance`) + "부모님께 결제 요청" 버튼. 만료일(expiry)은 목업엔 있지만 스키마에 대응 컬럼이 없어 — 언제 만료되는지는 비즈니스 규칙을 새로 정해야 하는 문제라 이번 스코프에서 제외. 결제 요청은 알림 인프라가 없어 단순 알림 문구로 끝내지 않고, `parent_requests`(원래 학부모→선생님 방향 요청용 테이블)에 실제로 행을 남기도록 구현 — 학생이 자기 보호자(`guardian_students`) 앞으로 요청을 쓸 수 있게 RLS를 확장. 실제 브라우저로 잔여 수업권 표시, 결제 요청 클릭 시 실제 DB insert(에러 없음), "OO 학부모님께..." 확인 문구까지 검증)
- [x] **025-student-problemlog**: 문제 탭 — 017의 `ProblemLogTab`(세션에 종속되지 않는 컴포넌트) 재사용, 학생 포털 최상위 탭으로 배치 (props만 연결, 새 코드 거의 없음. 세션뷰에서 만든 풀이 기록/선생님 픽/저장 상태가 그대로 보이고, 저장 토글도 실제로 동작하는 것까지 브라우저로 검증)
- [x] **026-student-vocab**: 단어장 탭 — 013의 `VocabTab`(세션에 종속되지 않는 컴포넌트) 재사용, 학생 포털 최상위 탭으로 배치 (props만 연결. 세션뷰 교재 탭에서 단어를 추가하면 학생 포털 단어장에도 즉시 반영되는 것까지 실제 Claude API로 브라우저 검증 — 013에서 의도한 "세션뷰와 학생 포털의 단어장 데이터 공유" 설계가 실제로 작동함을 확인)
- [x] **027-student-homework**: 과제 탭 — 세션 하나가 아니라 전체 세션에 걸친 과제 목록(제출 여부/점수) (목업대로 작성 필요/작성 완료 서브탭 + 과목·회차별 아코디언 그룹핑. 저장은 014의 `saveHomeworkAnswer` 서버 액션을 세션 무관하게 그대로 재사용(변경 없음). 목업의 "난이도 어땠나요" 자가평가 버튼은 새 컬럼이 필요해서 이번 스코프에서 제외. 실제 브라우저로 선생님이 과제 추가 → 학생 포털에 과목·회차로 그룹핑되어 노출 → 답안 작성 시 실제 DB 저장 및 "작성 완료"로 자동 이동 → 새로고침 후 유지까지 전체 흐름 검증)
- [x] **028-student-stats**: 통계 탭 — 020에서 만든 참여율 요약을 전체판으로 확장(수업 참여율/만족도/과목별 참여율). 목업의 "지각율"/"과제 사전 완료율"은 대응하는 스키마가 없어(지각 여부·과제 제출 시각을 기록하는 컬럼 자체가 없음) 024의 만료일 때와 같은 이유로 제외 — 새 컬럼을 만들려면 "무엇을 지각으로 칠지" 같은 비즈니스 규칙을 먼저 정해야 함. 만족도는 `session_student_feedback.rating` 평균, 과목별 참여율은 과목별 completed/no_show 비율로 실제 계산. 실제 브라우저로 참여율(2/2 완료=100%) 확인, 레슨 탭에서 실제로 별점을 제출한 뒤 통계 탭의 만족도 평균이 즉시 반영되는 것까지 검증

## Phase 3 — 학부모 포털

- [x] **030-parent-shell**: 자녀 전환, 대시보드 (목업 대비 사이드바를 4개 항목(홈/레슨/수업권/통계)으로 축소 — 커리큘럼/선생님프로필/리뷰는 학생 포털처럼 "레슨" 하위 서브뷰로 흡수 예정(031). 자녀 전환은 목업처럼 topbar pill 버튼으로 구현하되, `currentChild` 전역 변수 대신 `?child=` URL 쿼리로 관리해 새로고침/서버 렌더링에도 선택 상태가 유지되게 함(세션뷰의 `?tab=` 패턴과 동일). 홈 대시보드는 020에서 만든 `loadDashboardData`/`HomeDashboard`를 studentId만 "현재 선택된 자녀"로 바꿔 그대로 재사용 — 새 코드 없이 그대로 동작. 시드에 자녀가 지훈 하나뿐이라 자녀 전환을 검증할 데이터가 없었어서 둘째 자녀(이서아, AP Calculus BC·이도현)를 guardian_students+enrollment+session으로 추가. 실제 브라우저로 자녀 전환(지훈 ↔ 이서아, 대시보드 데이터가 실제로 바뀜) → 새로고침 후 유지 → 예정된 수업 클릭 시 세션뷰에 "학부모 (읽기전용)" 배지로 정상 진입까지 검증)
- [x] **031-parent-lessons-records**: 지난 수업 기록 열람 (세션뷰 읽기전용 접근) (021에서 만든 `LessonsTab`/`CurriculumView`/`ReviewPanel`을 학생 포털과 거의 그대로 재사용 — 새 `readOnly` prop만 추가해 메모 작성 폼(`CurriculumView`)과 피드백 제출 폼(`ReviewPanel`, 대신 학생이 남긴 평가를 읽기전용으로 표시)만 숨김. 자녀 전환 시 레슨 탭의 서브뷰 상태가 이전 자녀 것으로 남아있지 않도록 `key={currentChildId}`로 강제 리마운트. 실제 브라우저로 지난 수업→커리큘럼→리뷰까지 읽기전용으로 열람, 자녀 전환 시 홈으로 강제 이동(목업과 동일) 및 데이터 전환까지 검증. 검증 중 실제 RLS 버그 발견/수정: 021에서 추가한 `teacher_curriculum_templates`/`teacher_curriculum_template_units`의 "수강 학생 조회" 정책이 학생 본인만 커버하고 보호자(`is_guardian_of`)는 빠져있어서, 학부모가 커리큘럼 뷰를 열면 "아직 배정된 커리큘럼이 없습니다"로 잘못 표시됐음)
- [x] **032-parent-credits-billing**: 수업권/결제 현황 (**중요한 스코프 제외**: 목업의 "결제 수단" 카드(카드번호 직접 입력·저장 UI)는 구현하지 않음 — 우리 스키마(`payment_methods.stripe_payment_method_id`)도 이미 Stripe 토큰만 저장하도록 설계되어 있고, 실제 카드 원본 데이터를 다루려면 Stripe Elements 같은 PCI-compliant 연동(071-stripe)이 필요함. 카드번호를 그대로 받는 입력 폼을 만드는 건 보안 원칙 위반이라 배제. 대신 실제로 구현 가능한 것들: 수업권 잔여(자녀별 `students.credit_balance`), `credit_packages` 실제 목록(구매 버튼은 "충전 결제는 아직 지원되지 않습니다" 안내로 대체, 071 대기), 추천 코드(`parents.referral_code` 실데이터 + 클립보드 복사). 만료일도 024/028과 같은 이유(스키마에 없음)로 제외. 실제 브라우저로 잔여 수업권/패키지/추천코드 표시, 자녀 전환 시 수업권 데이터도 함께 전환되는 것까지 검증)

## Phase 4 — 선생님 포털

- [x] **040-teacher-shell**: 사이드바, 스케줄 (functional-spec의 "선생님 포털은 초기엔 다듬어지지 않아도 됨" 원칙에 따라 학생/학부모 셸보다 가볍게 — 사이드바 7개 항목(홈/일정/수업/학생/커리큘럼/교재/정산) 배치하되 이번 티켓은 "홈"만 구현(캘린더+예정된 수업 위젯, 학생 포털 HomeDashboard와 동일 구조를 여러 학생/과목에 걸쳐 집계하도록 재구성). 목업의 "대기 중인 수업 신청" 위젯은 스케줄링을 Calendly로 확정하면서 이미 폐기된 개념(schema-draft.md 결정)이라 애초에 대상 아님. 시드에 선생님당 학생이 1명뿐이라 "여러 과목/세션에 걸친 스케줄"을 검증할 데이터가 없어서 박서연 선생님에게 지훈 앞으로 두 번째 과목(AP Statistics, 목업 TEACHERS 딕셔너리 조합 그대로) enrollment를 추가. 실제 브라우저로 여러 과목에 걸친 예정 수업 목록, 캘린더 상세보기, 세션뷰 진입(선생님 권한 확인)까지 검증)
- [x] **041-teacher-schedule**: 예정/지난 수업, 수업 준비/진행 → 세션뷰 진입 (040의 `loadTeacherDashboard`를 확장해 `past` 세션도 함께 수집(추가 쿼리 없음), 예정된 수업/지난 수업 서브탭으로 여러 학생·과목에 걸친 전체 목록을 표시. 커리큘럼 클릭 이동(042)과 리뷰 작성(043)은 각자 티켓 스코프라 이번엔 세션뷰 진입 버튼("수업 준비"/"수업 기록")만 구현. 실제 브라우저로 여러 과목에 걸친 예정/지난 수업 목록, 세션뷰 진입까지 검증)
- [x] **042-teacher-roster-curriculum**: 학생 로스터, 커리큘럼 편집 (회차 클릭 → 세션뷰) (목업 조사 결과 "커리큘럼" 화면은 실제로 두 레이어다 — ① `MY_SUBJECTS_T`: 선생님 개인 과목 템플릿(신규 배정 시 기본값, 이미 배정된 학생에는 영향 없음) ② `CURRICULA_T`: 학생에게 배정된 후의 사본. 다만 실제 스키마/021·031 구현은 이미 스냅샷이 아니라 `teacher_curriculum_template_units`를 세션이 그대로 라이브 조인하는 방식이었음 — 이 티켓에서 그 구조를 그대로 따르기로 결정(스냅샷 전환은 더 큰 스키마 변경이라 범위 밖으로 명시). "학생" 탭(`RosterTab`)은 담당 학생×과목별 진행 회차를 보여주고, 과목 칩 클릭 시 "커리큘럼" 탭의 "학생별" 서브탭으로 바로 이동(셸에 `curriculumJump` 상태로 전달). "커리큘럼" 탭은 "내 과목"(템플릿 CRUD — 이미 스키마에 완비된 `teacher_curriculum_templates`/`_units` RLS(`teacher_id=auth.uid()`)를 그대로 사용, 새 마이그레이션 불필요)과 "학생별"(배정된 커리큘럼 진행 상황 — 021에서 만든 `CurriculumView`/`ReviewPanel`을 그대로 재사용, `readOnly` 없이 호출해 선생님이 메모를 남길 수 있게 함)로 분리. 목업의 드래그 재정렬은 접근성/테스트 용이성을 위해 "↑ 위로"/"↓ 아래로" 버튼으로 의도적으로 단순화(명시적 UX 변경). 새 과목 템플릿 생성 시 관리자 과목 템플릿(`subject_template_units`)이 있으면 그대로 복제하고, 없으면(AP Statistics처럼 아직 관리자가 안 만든 과목) 빈 템플릿으로 시작. 리뷰 보기는 043(리뷰 작성)이 아직 없으므로 학생 만족도만 읽기 전용으로 노출(`ReviewPanel readOnly`). 진행 중 `app/student/memo-actions.ts`의 실제 버그 발견: `addMemo`가 `author_role`을 `"student"`로 하드코딩하고 있어 선생님이 메모를 남겨도 "학생"으로 표시됐음 — 호출자의 `profiles.role`을 조회해 실제 role로 저장하도록 수정(학생/학부모 포털 동작에는 영향 없음, 학부모는 항상 readOnly라 메모 작성 자체를 안 함). `supabase db reset` 로컬 검증 + 유닛 테스트 22개 신규(`RosterTab`/`MySubjectsTab`/`CurriculumTab`) 통과, 전체 123개 통과, `tsc --noEmit` 클린. 실제 브라우저로 박서연 선생님 로그인 후: 로스터에서 지훈의 SAT Math 칩 클릭 → 커리큘럼 학생별 뷰로 바로 진입 확인, 메모 추가 시 "선생님"으로 정상 표시 확인, 리뷰 보기에서 학생 피드백 폼이 안 보이는 읽기 전용 확인, "내 과목"에서 AP Statistics 템플릿 신규 생성(빈 템플릿, 관리자 카탈로그 없음) → 회차 추가 → 제목 인라인 편집 → 아래로 이동(순서 스왑) → 삭제까지 전부 서버 로그로 실제 서버 액션 호출 확인. 새 마이그레이션 없음(RLS가 이미 완비되어 있었음), 새 시드 데이터 없음(기존 지훈/AP Statistics 무템플릿 상태를 그대로 빈 템플릿 생성 케이스로 활용))
- [x] **043-teacher-review**: 수업 리뷰 작성 (카테고리별 AI 초안 + 검토완료 체크) (목업(`openReviewAuthoring`)은 4개 카테고리(개념 이해도/문제 해결 능력/수업 참여도/과제 수행도)에 하드코딩된 고정 문구를 흉내만 냈지만, 표준 지침대로 실제 Claude Sonnet 5 API로 바로 연동 — `app/teacher/review/[sessionId]/review-actions.ts`의 `generateReviewDraft`가 세션의 과목/회차/단원/선생님 메모·코멘트/배정된 과제(homework_items) 채점 현황을 프롬프트에 실제로 넣어 tool-use로 카테고리 4개 텍스트 + teacherSummary/strength/improve/nextPlan을 한 번에 생성(015의 tool_choice 강제 패턴 재사용). "검토완료" 체크박스는 목업과 동일하게 제출을 막지 않는 카테고리별 표시 전용 상태(`session_review_categories.reviewed`)로 구현 — 목업도 게이팅하지 않으므로 그대로 따름. AI 초안(`ai_draft_text`)은 015와 동일하게 DB에 저장하지 않고 편집 가능한 클라이언트 상태로만 유지, "리뷰 제출" 시에만 `final_text`+`submitted_at`으로 확정(초안 상태에선 학생/학부모에게 노출 안 됨 — `app/student/review-data.ts`가 `submitted_at is not null`만 읽는 기존 동작과 정합). 목업의 "학부모님께" 별도 메시지 필드는 실제 스키마(`session_reviews`)에 대응 컬럼이 없어 제외, 대신 스키마에 있는 `strength`/`improve` 입력창을 추가(목업에 없던 필드지만 스키마와 학생용 ReviewPanel이 이미 소비하고 있어 UI 누락이었던 것으로 판단, 이번에 채움). `session_review_revisions`(재제출 이력 스냅샷)는 관리자 전용 insert RLS라 이번 스코프에서 제외. 진입점은 041에서 스코프를 미뤄뒀던 대로 `ScheduleTab`의 지난 수업 행에 "리뷰 작성"/"리뷰 수정"(기존 제출 여부로 라벨 분기) 버튼을 새로 추가해 `/teacher/review/[sessionId]` 라우트로 진입. 새 마이그레이션 없음(session_reviews/session_review_categories 모두 최초 스키마부터 `session_teacher_id(session_id)=auth.uid()` 쓰기 정책 완비). 유닛 테스트 7개 신규(ScheduleTab 리뷰 버튼 3개 + TeacherReviewPanel 4개), 전체 130개 통과, tsc 클린. 실제 브라우저로 박서연 선생님 로그인 후 7회차(SAT Math) 리뷰 작성 진입 → "AI 초안 전체 생성" 클릭 시 실제 Claude API 호출(서버 로그로 9초 소요 확인, mock 아님) → 세션의 teacher_comment("실수 유형 확인 회차")와 과제 없음 상태를 실제로 반영한 자연스러운 한국어 텍스트 생성 확인 → 4개 검토완료 체크 → 제출 → 서버 로그로 submitReview 확인 → ScheduleTab에서 "리뷰 작성"→"리뷰 수정"으로 라벨 전환 확인 → 지훈 학생 계정으로 재로그인해 학생 레슨 탭에서 동일 리뷰가 "(미작성)" 표시 없이 4개 카테고리 전부와 종합 리뷰(총평/잘한 점/보완할 점/다음 계획)까지 정확히 노출되는 것을 학생 화면에서 직접 확인)

## Phase 5 — 관리자 포털 (첫 런칭엔 러프해도 됨, 나중 다듬기)

- [x] **050-admin-shell**: 사이드바, 대시보드 (목업 사이드바 9개 항목(홈/사용자/매칭/상담/커리큘럼/수업권/계약/QC/정산/문서, 문서는 목업엔 10번째로 별도 존재해 실제로는 10개)을 그대로 배치하고 이번 티켓은 홈(대시보드)만 구현 — 020(학생 셸)이 확립한 "전체 nav 배치 + 이번 스코프만 구현" 패턴을 그대로 따름. 051(과목 템플릿)/052(교재 편집기)/053(교재 라이브러리)는 목업상 전부 "커리큘럼" nav 하나에 서브탭으로 들어가고, 054(유저·빌링)는 "사용자"/"수업권" 두 nav에 걸침 — 매칭/상담/계약/QC/정산/문서는 이번 phase 티켓 범위 밖(각각 072/074 등 후속 티켓이거나 아예 미티켓 영역)이라 준비 중 문구만 표시. 대시보드는 목업의 5개 카드 중 실제 스키마로 정직하게 집계 가능한 것만 구현: 상담 요청 대기(consult_requests status=requested)/확정된 상담 일정(status=confirmed)/학생 매칭 대기(students.status=pending)/선생님 승인 대기(teachers.status=pending, 목업엔 없지만 실제로 존재하는 유일한 승인 대기 컬럼이라 대체)/QC 경고 현황(teacher_qc_warnings group by teacher). 목업의 "초대 대기"·"결제 실패" 카드는 대응하는 실제 테이블·컬럼이 없어 제외(020/024 선례와 동일하게 스키마에 없으면 트림). 계정 메뉴는 다른 포털과 달리 "홈으로"(대시보드로 복귀) 항목이 하나 더 있는 것도 목업 그대로 구현. 시드에 관리자 대시보드 검증용 데이터(상담 요청 2건, 승인 대기 선생님/학생 각 1명, QC 경고 2건) 추가 — RLS는 모든 관련 테이블에 `is_admin()` 전체 권한이 최초 스키마부터 이미 열려 있어 새 마이그레이션 불필요. 유닛 테스트 6개 신규, 전체 136개 통과, tsc 클린. 로컬 Supabase + 실제 브라우저로 관리자 계정 로그인 후 5개 카드 전부 시드 데이터와 정확히 일치하는 것 확인, 사이드바 탭 전환/준비중 문구/계정메뉴 "홈으로" 왕복까지 확인)
- [x] **051-admin-subject-template**: 과목 템플릿 CRUD (다른 화면의 선택지 소스) (목업의 "커리큘럼" nav는 4개 서브탭(과목 템플릿/교재 문서/교재 라이브러리/승인 대기)으로 구성 — `CatalogTab.tsx`에 전부 배치하고 이번 티켓은 "과목 템플릿"만 구현(052/053/054는 각자 티켓). `subjects`(과목명)/`subject_template_units`(회차: unit_title/note/position) CRUD를 042의 `MySubjectsTab` 패턴(인라인 수정, 위/아래 이동, TEMP_OFFSET 3단계 position swap)으로 재사용해 구현하되, 042와 달리 이번엔 과목 레코드 자체의 생성/이름수정/삭제까지 포함. 과목 삭제는 `teacher_curriculum_templates`/`enrollments`/`curriculum_docs`/`problems`가 `subject_id`를 참조 중이면 FK 위반(23503)이 나므로, 이를 잡아 "이미 선생님 커리큘럼/매칭/교재 등에서 사용 중이라 삭제할 수 없습니다" 안내로 변환(실제 SAT Math 삭제 시도로 이 경로를 브라우저에서 직접 확인). 목업의 회차별 "교재 연결/보충 자료/과제" 서브 UI는 052(교재 편집기)·053(교재 라이브러리) 완료 전에는 의미가 없어 이번 스코프에서 제외(unit_title/note/순서만). 과목 추가는 목업의 `prompt()` 대신 인라인 입력창으로 구현(이 코드베이스 전체에서 네이티브 prompt/confirm을 쓰지 않는 기존 관례를 따름), 과목 삭제도 네이티브 confirm 대신 인라인 확인 UI. 코드 전수조사 결과 `subjects`/`subject_template_units`를 하드코딩 대신 실제로 조인해서 읽는 기존 화면들(042/021/023/015 등)이 이미 "단일 진실 소스" 원칙을 지키고 있음을 재확인 — 051은 그 소스에 관리자 쓰기 경로를 추가하는 작업. 새 마이그레이션 없음(두 테이블 모두 최초 스키마부터 `is_admin()` 전체 쓰기 정책 완비). 유닛 테스트 12개 신규, 전체 144개 통과, tsc 클린. 로컬 Supabase + 실제 브라우저로 관리자 계정에서 시드된 11개 과목·SAT Math 5개 회차 정상 표시 확인, SAT Math 삭제 시도 시 FK 에러 안내 확인, AP Biology 이름 수정+회차 추가, 신규 과목(AP World History) 추가까지 서버 로그로 실제 서버 액션 호출 확인)
- [x] **052-admin-curriculum-doc-editor**: 교재 편집기 — WYSIWYG(contenteditable), 문제 생성(AI) 연동 (신중하게 조사 후 진행 — 목업(`cat-docs`)은 제목+과목+단원(051의 `subject_template_units`) 선택 → 바로 에디터 진입 2단계 플로우, 좌측이 섹션(=목차) 목록 겸 편집 영역인 구조를 그대로 따름. `RichTextEditable.tsx`로 섹션 본문/티칭팁 둘 다에 재사용 가능한 contenteditable+`document.execCommand` 툴바 구현 — 목업의 색상피커/표삽입/강조박스는 관리 포털 초기 러프 허용 원칙에 따라 제외하고 굵게/기울임/밑줄/H2/H3/본문/목록 2종만 구현(의도적 축소). 문제 생성(AI)은 015와 달리 특정 세션이 아니라 섹션에 귀속된 재사용 가능한 "문제 은행"을 만드는 구조임을 시드 데이터(`section_id` 사용, `origin_session_id` null)로 재확인하고, 015의 tool-use 강제 패턴을 그대로 재사용해 실제 Claude Sonnet 5로 즉시 연동(초안은 DB에 저장하지 않고 "문제로 추가" 확정 시에만 `problems.status='confirmed'`로 insert, 015와 동일한 초안→확정 원칙). 게시는 관리자 작성 문서만 스코프로 하여 draft↔published 단순 토글로 구현 — 목업의 "선생님 작성 → 승인 대기 → 관리자 승인/반려" 흐름은 선생님 포털에 교재 작성 진입점 자체가 없어(어느 티켓에도 배정 안 됨) 이번 스코프에서 제외(사람 확인 없이 판단, 커밋 메시지에 근거 명시). **보안 이슈 발견 및 즉시 수정**: 목업/기존 코드(`LibraryDocView`, `MaterialTab`)가 섹션 body를 `dangerouslySetInnerHTML`로 sanitize 없이 그대로 렌더링하고 있었는데, 지금까지는 SQL 시드(신뢰된 관리자)로만 채워져 위험이 없었지만 052로 브라우저 contenteditable→서버 저장 경로가 생기면서 저장형 XSS 위험이 실제로 발생 — `sanitize-html` 패키지를 신규 설치해 `lib/sanitize-doc-html.ts`에서 허용 태그 화이트리스트로 저장 직전(서버 액션) 한 번 걸러내도록 구현. 브라우저에서 `<script>`+`onerror` 페이로드를 실제로 주입해 DB에 스크립트 태그 없이 안전하게 저장되는 것까지 직접 확인. `problems` 테이블에 admin 전용 delete 정책이 없어(RLS 기본값=거부) 문제 삭제 UI가 동작하지 않는 걸 발견해 새 마이그레이션(`20260829040000_problems_admin_delete.sql`)으로 추가. 051의 `moveSubjectUnit`(TEMP_OFFSET 3단계 position swap) 패턴을 섹션 순서변경에 그대로 재사용. 실제 조사 중 진짜 버그 하나 발견: `curriculum_docs`가 `subject_template_units`와 두 개의 서로 다른 관계(직접 FK `unit_id` + `subject_template_unit_materials` N:M)를 가지고 있어 PostgREST 임베드 쿼리가 모호성 에러(PGRST201)를 반환했는데, 로더 코드가 `error`를 확인하지 않고 조용히 빈 배열을 반환해 "교재 목록이 텅 빈 것처럼" 보이는 버그로 이어짐 — FK 제약 이름을 명시(`subject_template_units!curriculum_docs_unit_id_fkey`)해 해결(실제 브라우저에서 재현 후 직접 수정). 유닛 테스트 24개 신규, 전체 157개 통과, tsc 클린. 로컬 Supabase + 실제 브라우저로 관리자 계정에서: 시드된 2개 교재의 섹션/티칭팁/기존 문제 정상 표시 확인 → 섹션 본문 굵게 서식 적용 확인 → AI로 실제 문제 3개 생성(서버 로그로 8초 소요 확인, mock 아님) 후 문제 은행에 확정 추가 → XSS 페이로드 sanitize 확인 → 배포/배포취소 토글 확인 → 섹션 추가 확인 → "새 교재 만들기"로 AP Statistics(단원 없음, 안내문구 확인) 교재 신규 생성 후 바로 에디터 진입까지 전부 확인. 마이그레이션 호스팅 프로젝트에 push 완료)
  - [x] 교재 문서 편집기 업그레이드: 섹션 타입(개념 설명/문제 생성) 구분, 문제 포맷별(객관식 5지선다/서술형/수학 화이트보드형) 전용 저작 UI, 표 삽입, 문서 삭제 기능 (2026-08-29)
- [x] **053-admin-material-library**: 교재 라이브러리 (과목→단원→교재 폴더 탐색) (목업 조사 결과 052(교재 문서 목록)와 053(라이브러리)은 같은 원본 데이터를 다른 목적으로 보여주는 별개 화면임을 확인 — 052는 전체 교재(초안 포함)를 평평하게 나열해 편집 진입, 053은 published 교재만 과목→단원 2단 폴더 트리로 "탐색"만 하고 클릭 시 편집이 아니라 023이 이미 만든 읽기 전용 `/materials/[id]` 뷰어로 진입(새 창). 단원이 없는 교재는 목업 그대로 "(단원 미지정)" 폴더에 담음. 052에서 이미 로드해둔 `docs`(전체 교재+섹션+문제)를 그대로 재사용해 클라이언트에서 published 필터+과목/단원 그룹핑만 수행 — 새 데이터 로더/쿼리 불필요. 023의 학생용 교재 라이브러리(`materials-data.ts`)는 과목 레벨까지만 그룹핑하고 단원 트리가 없어 그대로 재사용은 안 되고, 새 컴포넌트(`MaterialsLibraryTab.tsx`)로 구현하되 `/materials/[id]` 뷰어 자체는 role-agnostic이라 그대로 재사용. 목업의 "N명이 가져다 씀" 통계와 검색/필터는 053 화면 자체에 없어(052에만 있음) 스코프에서 제외. "052 에디터로 편집 진입" 동작은 목업 스펙에 없어(순수 탐색+뷰어 진입만) 구현하지 않음. 새 마이그레이션/로더 불필요. 유닛 테스트 4개 신규, 전체 162개 통과, tsc 클린. 실제 브라우저로 관리자 계정에서 SAT Math 폴더 → 단원별 그룹핑(이차방정식과 이차함수 / (단원 미지정)) → 교재 링크 클릭 → `/materials/[id]` 뷰어 정상 진입까지 확인)
  - [x] 교재 라이브러리 화면의 문제 답 유출 버그 수정 + 실제 풀이 UI(객관식 클릭채점/서술형 자동확장 타이핑/수학 캔버스) 적용, 붙여넣기 서식(색상/배경/표) 보존, 목차 마지막 섹션 스크롤 버그 수정 (2026-08-29)
- [x] **054-admin-users-billing**: 학생/선생님 계정 관리, 수업권 조정 (목업 조사 결과 "사용자" nav는 학부모/학생/선생님 3개 서브탭, 상태변경은 상세화면에만(목록엔 없음), 승인 액션도 050 대시보드가 아니라 054 사용자 상세에 있음을 확인 — 050 대시보드는 그대로 두고(목록 표시만) 054의 학생/선생님 상세에서 실제 상태변경 UI 구현. `teacher_status` enum이 `active/pending` 2개뿐이라 목업의 3단계 드롭다운을 선생님은 2단계로 축소. "수업권" nav는 목업처럼 가격 카드(읽기전용) + 학생별 잔액 인덱스이고, 실제 조정 UI/이력은 학생 상세와 공유 — `StudentDetailPanel.tsx`를 UsersTab/BillingTab 양쪽에서 재사용. 조정 카테고리는 목업의 "환불/굿윌/오류정정/기타" 4종이 실제 `credit_tx_type` enum(purchase/debit/refund/adjustment/referral_bonus)과 정확히 안 맞아 환불→refund, 나머지(굿윌/오류정정/기타)→adjustment로 매핑(사유는 자유 텍스트에 그대로 기록). 조정은 `students.credit_balance` 직접 update + `credit_transactions` insert(append-only 이력, update/delete RLS 자체가 없어 이력 수정 불가 — 의도된 설계 그대로 따름). **신규 계정 생성은 목업의 가짜 시뮬레이션(`INVITES_T` 배열에 push만 하고 실제 발송 없음) 대신 실제 Supabase Auth Admin API(`auth.admin.inviteUserByEmail`)로 즉시 실연동** — 073(카카오/이메일 알림)을 기다릴 필요 없이 Supabase 기본 초대 이메일이 실제로 발송됨을 로컬 Mailpit으로 확인(073은 카카오 알림톡 등 브랜딩된 채널 완성이 스코프이지 "이메일 발송 자체"의 전제 티켓이 아니었음을 재확인). service_role 키를 쓰는 관리자 전용 클라이언트(`lib/supabase-admin.ts`)를 신규 도입 — auth.users 이메일 조회에도 재사용(profiles/students/teachers 테이블엔 이메일이 없어 이 방법 외엔 표시 불가). 학생 초대는 목업처럼 학부모 연결이 필수라 학부모 선택 드롭다운 포함, 초대 즉시 `guardian_students` insert까지 서버 액션에서 함께 처리. 051(`requireAdmin` 가드)과 052 패턴을 그대로 재사용. 새 마이그레이션 불필요(students/teachers/parents/guardian_students/credit_transactions 모두 최초 스키마부터 관리자 update/insert 권한 완비, 사전 조사로 재확인). 유닛 테스트 16개 신규, 전체 178개 통과, tsc 클린. 실제 브라우저로 관리자 계정에서: 학부모/학생/선생님 목록에 실제 이메일까지 정상 표시 → 지훈 수업권 5장 조정(14→19장, 이력 표시, DB 영속 확인) → 박서연 QC 경고 이력 2건 상세 표시 → 정하나 선생님 승인(pending→active) → 신규 선생님 초대 후 실제 Mailpit에서 "You've been invited" 이메일 수신 확인 → 수업권 탭에서 페이지 새로고침 후 조정된 잔액이 정확히 반영된 것까지 확인)

## Phase 6 — AI 실연동

- [x] **060-ai-integration**: 목업에서 `setTimeout`으로 흉내낸 AI 호출(문제생성, 리뷰 초안)을 실제 Claude API 호출로 교체 (감사 결과 별도 작업 불필요 — 015(선생님 세션뷰 문제생성)와 043(선생님 수업 리뷰 초안)에서 각 티켓 진행 중 이미 선행 실연동 완료됐고, 052(관리자 교재 문제생성)도 015 패턴을 재사용해 실연동됨. 013(단어 뜻풀이)까지 포함해 `@anthropic-ai/sdk`를 쓰는 4개 지점 전부 실제 모델 호출(`claude-sonnet-5` 3곳, `claude-haiku-4-5-20251001` 1곳) 확인, 하드코딩된 키 없음, `app/` 전체에 AI 관련 `setTimeout`/mock 잔재 없음(테스트 파일의 `vi.mock`은 정상적인 유닛테스트 목킹). 013만 초안→확정 2단계가 없는데, functional-spec.md상 단어 뜻풀이는 선생님 검수가 필요한 콘텐츠가 아니라 사전적 조회에 가까운 즉시완결형 기능으로 명시돼 있어 의도된 차이임을 재확인. 코드 변경 없이 체크박스만 정리)

## Phase 7 — 외부 서비스 연동

- [x] **070-calendly**: 상담/수업 예약 임베드 (2026-08-28: `official@alton.education` 계정으로 실제 Calendly 연동 완료. 랜딩페이지(075) `#consult` 섹션에 `CalendlyWidget.tsx`(공식 inline embed, `next/script`로 위젯 스크립트 로드)를 075에서 만든 `ConsultForm`보다 먼저 배치 — "바로 예약"(Calendly)을 1순위, "시간 정하기 전에 문의만"(기존 폼)을 대체 경로로 유지(폼을 걷어내지 않고 병행 — 예약 확정 안 하고 그냥 문의만 남기고 싶은 방문자도 있을 수 있어서). 실제 이벤트 타입("1 Hour Meeting", `official-alton/30min`)의 location이 이미 `zoom_conference`로 설정돼 있어 076(Zoom 결정)과 자연스럽게 맞아떨어짐(사용자가 Zoom 연동까지 미리 끝내둠). 예약이 실제로 성사되면 Calendly가 `app/api/webhooks/calendly/route.ts`로 `invitee.created` 웹훅을 보내 `consult_requests`에 `status='confirmed'` + `scheduled_at`/`calendly_event_uri`까지 실제로 채워 넣도록 구현(스키마에 이미 있던 두 컬럼이 마침내 실제로 쓰이게 됨) — HMAC 서명 검증(`Calendly-Webhook-Signature: t=...,v1=...`)도 구현했고 서명 키 미설정 시(로컬 개발) 검증을 생략하도록 처리. 웹훅 "구독" 자체는 Calendly가 공개 https 콜백 URL을 요구해 로컬에서는 등록 불가 — `scripts/register-calendly-webhook.mjs`를 준비해뒀고 Vercel 배포 후 1회 실행하면 됨(그때 나오는 signing_key를 `CALENDLY_WEBHOOK_SIGNING_KEY`에 채우는 것까지가 마지막 단계). API 토큰은 사용자가 처음 붙여넣은 값이 전송 중 깨져 있었던 걸 JWT payload 디코딩으로 발견 → 재발급받아 실제 `/users/me`, `/event_types` 호출로 유효성 확인 후 저장. 새 마이그레이션 불필요(consult_requests가 이미 필요한 컬럼을 다 갖고 있었음). 후속 개선(같은 날): 위젯을 ALTON 브랜드 다크 테마로 커스터마이징(`background_color`/`text_color`/`primary_color` embed 파라미터). 사용자 피드백("데이터를 따로 받는 게 이상해 보인다")을 반영해 Calendly 위젯이 있을 땐 `ConsultForm`을 동시 노출하지 않고(중복 데이터 수집 제거), `NEXT_PUBLIC_CALENDLY_URL` 미설정 시에만 폼이 뜨는 진짜 폴백으로 전환 — 대신 학생 이름/학년/거주지역/고민 같은 정보는 Calendly 이벤트 타입의 커스텀 질문으로 받도록 안내(대시보드에서 직접 추가해야 함, API로는 이벤트 타입 질문을 쓸 수 없음). 웹훅은 질문 텍스트에 "학년"/"연락처(전화)"가 포함되면 각각 `student_grade`/`phone` 컬럼에 매핑하고 나머지는 `concerns`에 모으도록 확장. "Powered by Calendly" 배지는 임베드 파라미터로 못 지우고 Calendly 계정의 Branding 설정(유료 플랜)에서만 가능해 사용자가 직접 처리하기로 함. 유닛 테스트 1개 추가(학년/연락처 매핑), 전체 188개 통과, tsc 클린. 실제 브라우저로 다크 테마 위젯 렌더링 확인, E2E 12개 전부 통과(단, 로컬 dev 서버 1개에 6-worker 동시 로그인 시 타임아웃 플레이크 발견 — worker 수를 줄이면 전부 통과, 코드 회귀 아님을 확인). **후속 확장(같은 날, 원래 070 스코프에 있었으나 미착수였던 "개별 회차 예약"까지 완성)**: 학생↔담당 선생님 간 정식 회차 예약을 Calendly로 구현 — Calendly Teams 체험판을 활용해 선생님마다 팀원 초대 후 개인 이벤트 타입(개인 예약 링크)을 만드는 구조로 결정(무료 플랜은 이벤트 타입 1개/팀원 초대 불가라 체험 기간 안에 구조부터 잡아둠, 유료 전환은 사용자가 추후 결정). `teachers.calendly_scheduling_url` 컬럼 추가(마이그레이션 `20260829050000`), 054 관리자 화면(`TeacherDetailPanel`)에서 설정. 학생(및 학부모) 포털의 `LessonsTab`에 담당 선생님별 "다음 회차 예약하기" 버튼 추가 — 클릭 시 그 선생님의 개인 예약 링크를 `CalendlyWidget`으로 열되 `?utm_content=<enrollmentId>`를 실어 보냄. 웹훅(`route.ts`)은 `tracking.utm_content`가 있으면 상담 신청이 아니라 세션 예약으로 분기해 `sessions`에 새 회차를 insert(session_number는 기존 최댓값+1로 계산해 유니크 제약 충돌 방지, Zoom 조인 링크는 `scheduled_event.location.join_url`, 소요 시간은 start/end_time 차이로 계산)하고 `enrollments.current_session`도 함께 갱신. 실제 브라우저로 박서연 선생님의 예약 링크를 저장 → 지훈 학생 포털에서 위젯이 올바른 utm_content로 열리는 것 확인 → curl로 실제 웹훅 페이로드를 보내 9회차 세션이 실제로 생성되고 학생 포털 레슨 목록/진행률(9/12회차)에 즉시 반영되는 것까지 종단 간 확인. 다만 이 세션-예약 경로 자체는 실제 두 번째 Calendly 팀원 이벤트 타입으로 라이브 테스트한 건 아니고(사용자가 아직 선생님을 팀원으로 초대하지 않음) 웹훅 payload 형태에 대한 지식 기반 구현이라, 실제 선생님 계정이 붙으면 필드명(`scheduled_event.location.join_url` 등)이 정확히 맞는지 첫 실사용 시 재확인 필요. 유닛 테스트 3개 신규(관리자 URL 저장, 웹훅 세션-예약 분기 2종), 전체 191개 통과, tsc 클린. 새 마이그레이션 호스팅 프로젝트에 push 완료.)
- [ ] **076-zoom-recording-summary**: Zoom 클라우드 레코딩 자동 요약 → 수업 리뷰 첨부 (2026-08-28 신규 추가 — 원래 스펙엔 없었음. 화상 수업 플랫폼을 Google Meet에서 Zoom으로 변경한 이유이기도 함: Google 계정이 없는 학생·학부모도 있을 수 있어 Zoom으로 통일. 수업 종료 후 Zoom 클라우드 레코딩(또는 자동 생성 자막/트랜스크립트)을 API로 가져와 Claude로 요약, 043(수업 리뷰) 화면의 적절한 위치(하단 등)에 자동 첨부하는 기능. Zoom 계정(Pro 이상, 클라우드 레코딩 활성화) + Zoom API/webhook 연동이 선행되어야 함 — 녹화에 대한 학생/학부모 동의 고지 문구도 함께 챙겨야 함)
- [x] **071-stripe**: 수업권 결제 (2026-08-28: Stripe Checkout(호스팅 결제 페이지) 방식으로 구현 — 카드 원본 데이터는 우리 서버/코드에 절대 닿지 않음. 학부모 포털 수업권 탭(`CreditsTab.tsx`)의 패키지 카드마다 "충전하기" 버튼 추가 → 서버 액션 `app/parent/credits-actions.ts`의 `createCreditCheckoutSession`이 `credit_packages` 가격으로 Stripe Checkout Session을 생성(`metadata`에 student_id/package_id/parent_id 포함)하고 그 URL로 리다이렉트. 결제 완료는 `app/api/webhooks/stripe/route.ts`(`checkout.session.completed`)가 처리 — service_role 클라이언트로 `credit_purchases` insert(`stripe_payment_intent_id` 실제로 채워짐), `credit_transactions`에 `type='purchase'` 기록, `students.credit_balance` 증가까지 한 번에(스키마가 이미 이 흐름을 위해 설계돼 있었음, 새 마이그레이션 불필요). 성공/취소 시 `?tab=credits&purchase=success|cancelled` 쿼리로 돌아와 안내 배너 표시. 웹훅 서명 검증(`STRIPE_WEBHOOK_SECRET`)은 Stripe API로 프로덕션 URL(`https://alton-ecru.vercel.app/api/webhooks/stripe`)에 실제 엔드포인트(`we_1U9Z40ICtA5Uy7fuOFqPl5py`) 등록 후 받은 시크릿을 Vercel/로컬에 반영. 사용자가 제공한 test mode 키(`pk_test_.../sk_test_...`)를 로컬 `.env.local` + Vercel production 환경변수에 등록. 유닛 테스트 10개 신규(`CreditsTab`의 결제 버튼/상태 배너, `credits-actions`의 세션 생성/에러 처리, 웹훅 라우트의 지급/무시/누락-metadata 분기), 전체 198개 통과, tsc 클린. 같은 세션에서 Supabase(새 프로젝트로 마이그레이션 이관) + GitHub(`officialalton/alton`) + Vercel(GitHub 연동, 자동 배포) 인프라 전체를 처음 연결하고 프로덕션 배포까지 완료, SMTP(Gmail 앱 비밀번호)도 프로덕션에 등록해 이메일 알림(073)이 실제로 발송되도록 마무리. 실 카드가 아닌 Stripe 테스트 카드(`4242 4242 4242 4242`)로 결제 흐름 브라우저 종단 검증은 아직 미실시 — 다음 세션에서 확인 필요.)
- [x] **072-docusign**: 계약서 서명 (2026-08-28: 학부모/학생 계약만 구현, 선생님 계약(`teacher_contracts`)은 후속 티켓. DocuSign JWT Grant(RSA 키페어, Node `crypto`로 직접 서명 — SDK/새 의존성 없음) + 문서 직접 전송(anchor 태그 `/sig1/`, 템플릿 미사용) 방식. `lib/contracts/family-contract-template.ts`에 표준 약관 전문을 관리해 문구 수정이 그 파일 편집만으로 끝나도록 함(법률 검토 전 플레이스홀더 문구임을 주석으로 명시). 관리자가 "계약" 탭(`ContractsTab.tsx`)에서 상담 신청 건에 학생 이름/이메일을 입력해 "계약서 발송"을 누르면 `inviteParent`/`inviteStudent`(기존 로직 재사용, id를 반환하도록 수정) → `contracts` 행 생성(status='sent') → DocuSign 봉투 발송(`sendFamilyContract`, 발송 실패 시 contracts 행 생성 안 함). 서명 완료 웹훅(`/api/webhooks/docusign`)이 `contracts.status='signed'` + `students.status='active'`로 갱신, 모르는 envelope는 무시. 웹훅은 쿼리스트링 토큰(`DOCUSIGN_WEBHOOK_TOKEN`)으로 검증. **실제 계정 연동 중 발견한 사실**: 처음 받은 자격증명(User ID/API Account ID/Base URI)이 실제로는 프로덕션이 아니라 DocuSign 개발자(데모) 계정 것이었음(`account.docusign.com`으로 시도 시 "client id not registered" 에러) — `account-d.docusign.com`으로 전환 후 User ID/API Account ID가 실제로 서로 뒤바뀌어 있었던 것까지 콘솔 스크린샷으로 확인해 바로잡음(`DOCUSIGN_BASE_URI`도 `https://demo.docusign.net`으로 정정). 실제 JWT 토큰 발급 + `/oauth/userinfo` 호출로 계정("Alton Education") 확인 완료 — 실사용 전 DocuSign 콘솔에서 "Promote to production" 필요. Subagent-driven development로 진행(태스크 9개, 태스크별 구현+리뷰 서브에이전트), 리뷰 중 실제 결함 2건 발견 후 수정: (1) `sendFamilyContract`의 마지막 `consult_requests` 갱신 에러 미체크, (2) `ContractsTab`에서 발송 대기 건을 전환할 때 학생 이름/이메일 입력폼이 초기화 안 되는 문제. 리뷰 과정에서 Task 5(`inviteParent`/`inviteStudent` 반환 타입 변경)가 `UsersTab.test.tsx`의 tsc 에러를 만든 회귀도 발견해 수정(서브에이전트가 "기존 문제"로 오진단한 것을 직접 git log로 재확인 후 수정). 유닛 테스트 다수 신규(JWT 토큰 발급/캐싱, 봉투 생성, 계약서 템플릿, 발송 서버 액션, 웹훅, 발송 대기/계약 목록 조회, `ContractsTab` UI). **최종 전체 검토(final whole-branch review, Opus)에서 태스크별 리뷰가 못 잡은 교차 이슈 6건 추가 발견 후 전부 수정**: (1) 웹훅이 `DOCUSIGN_WEBHOOK_TOKEN` 미설정 시 검증을 생략(fail-open)하던 걸 항상 토큰이 일치해야만 통과하도록 변경(fail-closed), (2) `NEXT_PUBLIC_SITE_URL` 누락 시 조용히 `localhost`로 폴백해 실제 계약서에 죽은 콜백 URL이 박제되던 문제 → 미설정이면 에러를 던지도록 변경, (3) DocuSign/Stripe 환경변수가 `.env.example`에 전혀 문서화 안 돼 있던 것 추가, (4) `requireAdmin`이 `"use server"` 파일에 export돼 있어 의도치 않게 공개 Server Action이 돼버린 문제 → `lib/admin-auth.ts`로 분리, (5) 서명 anchor 문자열(`/sig1/`)이 두 파일에 각자 하드코딩돼 있던 걸 `SIGNATURE_ANCHOR` 상수로 단일화, (6) `contracts` 테이블에 정렬 가능한 컬럼이 없어(`id`가 UUID) "발송된 계약" 목록이 사실상 무작위 순서로 보이던 문제 → `created_at` 컬럼 추가 마이그레이션(`20260829060000`) + 최신순 정렬로 수정. 설계 문서도 실제 구현(계정 레벨 Connect 설정 없이 봉투별 웹훅 + 발급 토큰 검증, 원래 계획했던 HMAC 방식에서 변경)에 맞게 갱신. 전체 223개 테스트 통과, tsc 클린. **아직 남은 한계(다음에 실제 계약 보낼 때 확인 필요)**: DocuSign eventNotification의 `envelopeEventStatusCode` 값 대소문자(`"completed"`)가 실제 API 스펙과 맞는지 실사용 전 확인 필요(틀리면 서명 완료 콜백이 영원히 안 옴, 조용한 실패라 목업/유닛테스트로는 못 잡음) — 실제 브라우저로 계약서 발송→서명까지 종단 테스트 아직 안 함(DocuSign 개발자 계정 인증만 확인). 계정 초대 후 DocuSign 발송이 실패하면 그 이메일로 재시도 시 "이미 초대된 이메일" 에러가 나서 막힐 수 있음(계정 재사용/조회 로직 없음, 후속 개선 필요).)
- [x] **073-email-notify**: 학부모 알림 (이메일 전용 — Kakao Alimtalk 스펙 제외. 2026-08-28 결정: 카카오 비즈니스 채널 심사·템플릿 승인 등 대기 시간이 긴 외부 의존을 없애기 위해 알림은 이메일로만 발송하고, 실시간 소통은 022에서 이미 만든 서비스 내 채팅(chat_threads/chat_messages)으로 충분히 대체 가능하다고 판단. 특정 이메일 벤더에 종속되지 않도록 표준 SMTP(`nodemailer`, `lib/email.ts`)로 구현 — 운영에서는 `SMTP_HOST` 등 환경변수만 실제 제공자(Postmark/SendGrid SMTP 릴레이/Gmail Workspace 등) 것으로 바꾸면 되고, 로컬 개발은 `supabase/config.toml`의 `local_smtp.smtp_port`를 열어 Mailpit으로 실제 발송·수신을 확인할 수 있게 함. 이번 티켓에서 실제로 연결한 알림 트리거 2곳: (1) 랜딩페이지 상담 신청 접수 즉시 확인 메일(`app/consult-actions.ts`), (2) 선생님이 수업 리뷰를 제출하면(043) 학생의 보호자 전원에게 리뷰 도착 안내 메일(`app/teacher/review/[sessionId]/review-actions.ts`의 `notifyGuardiansOfReview`) — `guardian_students` 조회는 선생님 세션의 RLS로는 조회가 막혀 있어(당사자/관리자만 select 가능) service_role 클라이언트로 처리해야 했던 실제 버그를 브라우저 테스트 중 발견/수정(처음엔 이메일이 전송되지 않고 조용히 실패). 그 외 학생/과제/결제 관련 추가 알림 트리거는 이번 스코프에서는 확장하지 않음(필요 시 별도 확인). 새 마이그레이션 없음. 유닛 테스트 2개 신규(`lib/email.ts`), 전체 183개 통과, tsc 클린. 실제 브라우저로 상담 폼 제출 → Mailpit에서 실제 확인 메일 수신 확인, 선생님이 지훈의 SAT Math 7회차 리뷰 제출 → 학부모 김민지 실제 주소로 리뷰 도착 메일 수신까지 확인)
- [ ] **074-wise-payout**: 선생님 KRW 정산 (자동계산 + 사람 승인)
- [x] **075-landing-page**: 랜딩페이지(`alton_landing_v5.html` 목업 기반) 실제 구현 (Vercel 프로덕션 배포는 사용자 계정 로그인이 필요해 별도 진행 — 페이지 구현까지 완료) `app/page.tsx`를 create-next-app 기본 템플릿에서 실제 랜딩페이지로 전면 교체: 헤더(nav+로그인+상담신청 CTA)/히어로/Why Alton(3 pillar)/How it works(5단계, 다크 배경)/무료체험 콜아웃/FAQ(5문항)/상담신청 폼/푸터, 전부 목업 카피 그대로. `app/layout.tsx` 폰트를 Geist(영문 전용)에서 Inter+Noto Sans KR로 교체하고 lang="ko", 실제 메타데이터(title/description)로 갱신 — `globals.css`의 기존 색상 팔레트(세션뷰 기준 통일 팔레트)는 임의로 바꾸지 않고 그대로 재사용. **목업의 "상담 신청" 폼은 `onsubmit="event.preventDefault()"`로 실제 제출 로직이 아예 없는 순수 mock이었는데, 이 폼이 관리자 대시보드(050)가 읽던 `consult_requests` 테이블에 실제로 쓰는 유일한 경로라 075 스코프에 반드시 포함해야 한다고 판단해 실제 서버 액션(`app/consult-actions.ts`)으로 구현** — service_role 클라이언트(054에서 만든 `lib/supabase-admin.ts` 재사용, `consult_requests`는 관리자 전용 RLS라 익명 방문자가 직접 insert할 방법이 없으므로 새 공개 RLS를 여는 대신 서버 액션에서 service role로 처리). 스키마에 없는 "학생 이름"/"거주 지역" 필드는 전용 컬럼이 없어 `concerns` 텍스트에 구조화해 함께 기록(스코프 트리밍이 아니라 정보 손실 없이 저장하는 절충). 목업의 달력+시간슬롯 상담 예약 피커(하드코딩된 목데이터)는 070(Calendly)이 실제로 담당할 기능이라 이번 스코프에서 제외 — 대신 담당자가 나중에 연락드린다는 안내 문구로 대체. 새 마이그레이션 불필요. 유닛 테스트 3개 신규(ConsultForm), 전체 181개 통과, tsc 클린. 실제 브라우저로 전체 섹션 스크롤 확인, FAQ 토글 확인, 상담 폼 제출 → DB 실제 저장 확인 → 관리자 대시보드 "상담 요청 대기" 카드에 방금 제출한 신청이 실시간으로 뜨는 것까지 종단 간 확인)

## Phase 8 — 통합 테스트 및 파일럿 준비

- [x] **080-e2e-critical-paths**: 핵심 플로우 E2E (상담예약→계약→결제→매칭→수업→리뷰) — 원래 티켓명의 전체 체인(예약/계약/결제)은 070/071/072가 아직 미착수라 실제로 존재하지 않는 단계라, 지금 시점에 실제로 동작하는 핵심 경로들로 스코프를 잡아 Playwright E2E를 작성했다: (1) 랜딩페이지 로드+상담 신청 폼 제출→DB 실제 저장(075의 실제 서버 액션 경로), (2) 4개 역할(admin/teacher/student/parent) 로그인→각자 포털 리다이렉트, 다른 역할 경로 직접 접근 시 강제 리다이렉트, 로그아웃, (3) **가장 중요한 교차 역할 시나리오**: 선생님이 지난 수업 리뷰를 작성·제출하면 학생 포털에 그 내용이 그대로 보이는 것까지 한 테스트 안에서 검증(별도 브라우저 컨텍스트로 역할 전환) — 043(리뷰 작성) 티켓의 핵심 가치가 실제로 이어지는지 확인, (4) 관리자 과목 템플릿 생성+회차 추가, 대시보드 위젯 노출. `playwright.config.ts`의 `baseURL`/`webServer`가 기본 3000포트였는데 이 프로젝트의 실제 dev 포트(3010, `.claude/launch.json` 기준)와 달라 전부 실패하던 걸 발견해 수정. 유닛 테스트와 마찬가지로 로컬 Supabase 시드 데이터 기준으로 동작하며 실행 후 `supabase db reset`으로 복원 필요(테스트가 실제 DB에 리뷰 제출/과목 생성/상담 신청 등 실제 쓰기를 수행함). `npx playwright test`로 12개 전부 통과 확인.
- [x] **081-pilot-readiness**: 20명 파일럿 기준 버그바운스, 접근성 점검 (2026-08-28: 전체 유닛 테스트(229개) + Playwright E2E(12개) 재실행, 4개 포털(관리자/학부모/학생/선생님) + 랜딩페이지를 실제 로컬 브라우저로 전부 재점검, 랜딩페이지 모바일 반응형 확인. **실제 크리티컬 버그 1건 발견 후 수정**: `/set-password` 페이지가 초대/재설정 링크의 URL 해시(`#access_token=...`)를 세션에 명시적으로 반영하지 않아, 브라우저에 이미 로그인된 다른 계정(예: 방금 초대를 보낸 관리자 본인)의 세션에 그대로 비밀번호 변경이 적용되는 사고 — 로컬에서 admin 계정으로 실제 재현(관리자로 로그인된 채 신규 학부모 초대 링크를 열었더니 관리자 자신의 비밀번호가 바뀜) 후 `setSession()`으로 해시 토큰을 명시적으로 적용하도록 수정, 재현 시나리오로 수정 검증 완료. 같은 조사 과정에서 `inviteUserByEmail`의 `redirectTo`가 `/login`으로 가 있어(세션 인식 코드가 없는 페이지) 초대받은 사람이 애초에 비밀번호를 설정할 방법이 없었던 것도 `/set-password`로 함께 수정 — 이 두 버그는 사실상 전체 초대 온보딩 플로우(학부모/학생/선생님 초대, 072 계약 발송 시 계정 생성)를 막고 있던 문제라 파일럿 시작 전에 반드시 잡아야 했던 항목. 그 외 발견: 로그인 화면의 "선생님으로 지원하고 싶으신가요? 지원하기 →" 링크가 `href=\"#\"`인 죽은 링크(목업 단계부터 플레이스홀더였음, 실제 지원 폼 URL 필요 — 사용자가 구글 폼으로 준비하기로 함, 링크 받으면 연결 예정). Playwright E2E는 기존에 문서화된 "6-worker 동시 로그인 시 타임아웃 플레이크"가 1개 워커 초과 시 그대로 재현됐으나(worker=1로는 12개 전부 통과) 새로운 회귀 아님을 확인. 접근성 기본 점검(폼 라벨, 캘린더 버튼 접근성 이름, FAQ의 네이티브 `<details>` 사용 등) 문제없음. 겸사겸사 관리자 포털에 "개발 로그" 탭 추가(`docs/tickets.md`를 그대로 렌더링 — 별도 DB 테이블 없이 지금 있는 티켓 문서를 관리자 화면에서 바로 볼 수 있게 함, Stripe/DocuSign/Zoom 등 개인 계정으로 임시 연동된 항목들을 관리자가 한눈에 추적 가능). 유닛 테스트 6개 신규(`/set-password` 세션 처리 4개, `DevLogTab` 1개, `AdminShell` 개발 로그 탭 1개), 전체 229개 통과, tsc 클린. 프로덕션에 관리자 계정(`official@alton.education`) 초대 이메일 발송 완료 — 로그인 정보는 사용자가 직접 이메일에서 비밀번호 설정.)
## Phase 9 — 관리자 미착수 탭 + 확장 (2026-08-29 추가)

- [ ] **082-realtime-scratchpad**: 연습장 실시간 공동 문서 (진행 중 — 계획서 `docs/superpowers/plans/2026-08-29-realtime-scratchpad.md` 참고, 기존 "Docs" 서브탭의 외부 Google Docs 링크 붙여넣기를 자체 구현 실시간 텍스트 공동 편집으로 완전히 교체)
- [ ] **083-admin-matching**: 관리자 "매칭" 탭 정책 결정 + 구현. Airtable을 계속 쓸지, 우리 앱으로 가져올지부터 결정 필요(functional-spec.md엔 원래 Airtable로 명시돼 있었음)
- [ ] **084-admin-qc-tab**: 관리자 "QC" 탭 화면 기획 + 구현. `teacher_qc_warnings` 데이터는 이미 있고(대시보드 카드로만 노출 중), 전용 탭 화면이 없음
- [ ] **085-admin-consult-tab**: 관리자 "상담" 탭 화면 기획 + 구현. `consult_requests`는 이미 있고(대시보드 카드로만 노출 중), 상담 신청 건을 개별로 열어보는 상세 화면이 없음
- [ ] **086-admin-payouts-tab**: 관리자 "정산" 탭 — Wise 자동 송금 API 연동은 제외, 선생님별 시급×시간 기준 정산 필요 내역 계산/표시 + 관리자가 수기로 완료/완료취소 처리하는 UI. `teacher_payouts` 테이블은 이미 있음(설계 진행 중)
- [ ] **087-admin-documents-tab**: 관리자 "문서" 탭 — 회사 문서(법인설립/계좌정보/세무/계약템플릿 등) 업로드/다운로드 가능한 뷰. 구글 드라이브 연동되면 이상적(안 되면 Supabase Storage 등 대안)
- [ ] **088-admin-supervisor**: 관리자 하위에 "슈퍼바이저"(중간관리자) 역할 신설. 사용자 탭에 슈퍼바이저 섹션 추가, 초대 + 권한(어떤 관리 기능에 접근 가능한지) 설정 구조 필요

